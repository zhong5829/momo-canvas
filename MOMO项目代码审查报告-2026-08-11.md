# MOMO 智能画布项目代码审查报告

> 审查日期：2026-08-11  
> 审查范围：导演台、ComfyUI 模板与子工作流、批量生成、Skill、Ollama/GGUF、音频导演台、Premiere/XML/UXP 导出、项目持久化  
> 审查方式：代码静态审查、调用链追踪、TypeScript 类型检查、Vite 生产构建  
> 说明：本次只进行审查与验证，没有修改项目代码。

## 一、总体结论

当前版本的整体架构方向正确，导演台、ComfyUI、Skill、Ollama、资产库与剪辑交付已经形成较完整的产品骨架。类型系统和生产构建均可通过，但仍存在一些“界面和数据结构已经存在，实际执行链路没有接通”的问题。

当前不建议直接作为稳定版发布。建议先完成 P0、P1 问题修复，并使用真实 ComfyUI、Seedance/中转站和 Premiere 做端到端验证。

粗略完成度判断：

- 产品和界面骨架：约 80%。
- 真实业务闭环：约 55%～65%。
- 发布前主要风险：ComfyUI 模板污染、导演台输入未传入模型、配方不可配置、导出时间线不一致、UXP Bridge 不可用、临时视频地址丢失。

## 二、验证结果

### 已通过

- `npx tsc --noEmit`
- `pnpm build`
- Vite 共转换 426 个模块，生产构建成功。

### 构建警告

- 主业务包约 1.19 MB，Vite 提示部分 chunk 大于 500 kB。
- 多个模块同时存在静态导入和动态导入，相关动态导入无法形成独立 chunk。
- 这些警告暂时不是功能阻断项，但导演台稳定后可统一做代码分包。

## 三、P0：发布阻断问题

### P0-1 ComfyUI 模板会被运行过程永久改写

涉及文件：

- `src/core/services/comfy.ts:430`
- `src/core/services/comfy.ts:614`
- `src/core/services/comfy.ts:641-690`

问题说明：

`extractVariantWorkflow()` 将模板中的原始节点对象直接放入运行工作流：

```ts
out[id] = node;
```

随后 `runComfyTemplate()` 直接修改 `node.inputs`，包括提示词、图片文件名、视频文件名和公开参数。这些对象仍然属于存储中的模板，因此生成操作会污染模板本体。

可能造成：

- 第一次生成的提示词残留到第二次任务。
- 上一次上传到 ComfyUI 的图片文件名被重复使用。
- 两个并发任务互相覆盖图片、提示词或种子。
- 用户保存的模板默认参数被静默改变。
- 不同导演台片段出现串图、串提示词。

修复建议：

1. 在任何参数写入前深拷贝工作流。
2. 优先使用 `structuredClone()`，必要时提供 JSON 深拷贝降级。
3. `extractVariantWorkflow()` 返回的新对象不能复用模板节点引用。
4. 为此增加“运行前后模板 JSON 完全相等”的自动测试。

验收标准：

- 连续执行两次不同提示词和图片的工作流，第二次请求中不存在第一次输入。
- 同一模板并发执行两个任务时互不影响。
- 执行前后的 `tpl.workflow` 深度比较完全一致。

---

### P0-2 导演台的 ComfyUI 语义绑定没有接入实际生成

涉及文件：

- `src/core/comfyBindings.ts:144`
- `src/core/directorQueue.ts:104-112`
- `src/core/directorQueue.ts:162-170`

问题说明：

项目已经实现：

- `suggestBindings()`
- `validateBindings()`
- `resolveSlotWrites()`
- `workflowFingerprint()`
- `markVerified()`

但这些函数没有进入导演台生成调用链。

导演台执行 ComfyUI 配方时，只传入了：

- `recipe.defaultParams`
- `variantId`

没有传入当前片段实际编译出的：

- 正面提示词
- 负向提示词
- 首帧
- 尾帧
- 多参考图
- 参考视频
- 参考音频
- 站位图
- 姿势图

因此界面可能显示已经配置语义槽，但提交给 ComfyUI 的仍是模板默认值。

修复建议：

建立统一的 `DirectorInputBundle`：

```ts
type DirectorInputBundle = {
  prompt?: string;
  negativePrompt?: string;
  firstFrame?: string;
  lastFrame?: string;
  referenceImages?: string[];
  referenceVideo?: string;
  referenceAudio?: string;
  layoutGuide?: string;
  poseGuide?: string;
};
```

推荐执行流程：

1. 从项目规则、场景、片段、角色、Take 和 Skill 编译输入包。
2. 读取配方对应的模板和子工作流。
3. 调用 `validateBindings()`。
4. 调用 `resolveSlotWrites()` 生成精确写入目标。
5. 在深拷贝后的工作流中写入对应节点。
6. 提交 ComfyUI。
7. 将最终输入和绑定快照写入 Take，方便追溯。

验收标准：

- 导演台修改片段提示词后，ComfyUI 请求体对应节点值同步改变。
- 首帧和尾帧能分别进入指定节点，而不是依赖 LoadImage 编号猜测。
- 多参考图按照绑定索引分别写入，不发生“全部覆盖第一个输入”。
- 站位图/姿势图能通过语义槽传入工作流。

---

### P0-3 生成配方无法完整配置，也无法真正分配给片段

涉及文件：

- `src/modules/director/GenerationPage.tsx:39-48`
- `src/modules/director/GenerationPage.tsx:126-140`
- `src/core/directorQueue.ts:66-70`
- `src/core/directorQueue.ts:241`

问题说明：

“新增配方”只会创建一个没有 `templateId` 的空 ComfyUI 配方。当前界面只能显示配方，无法编辑：

- 配方名称。
- 本地 ComfyUI 或远程 API。
- ComfyUI 模板和子工作流。
- 远程服务商与具体模型。
- 文生视频、图生视频、首尾帧、多参考等模式。
- 默认参数、时长、分辨率、帧率。
- 当前片段使用哪个配方。
- 项目默认配方。

`resolveRecipe()` 的注释写着“片段配方 > 项目默认”，但实际没有项目默认回退。批量任务还全部硬编码为 `video`。

影响：

- 无法可靠切换 MiniMax H3 本地 ComfyUI、Seedance 中转站和其他第三方模型。
- 新建配方后批量生成仍可能走默认远程视频模型。
- 图片配方无法进入批量执行。

修复建议：

增加配方编辑器，至少包含：

1. 引擎：ComfyUI / Provider。
2. 输出：图片 / 视频。
3. 模式：t2i、i2i、t2v、i2v、首尾帧、多参考。
4. 模板与子工作流选择。
5. Provider 与模型选择。
6. 默认参数。
7. 能力摘要与不兼容提示。
8. “设为项目默认配方”。
9. 片段级配方选择。
10. 提交前预检。

验收标准：

- 同一项目中两个片段可以分别选择本地 ComfyUI 和 Seedance 中转站。
- 没有片段级配方时能够回退项目默认配方。
- 图片配方生成图片 Take，视频配方生成视频 Take。
- 切换配方时明确提示首帧、尾帧、多参考等能力是否兼容。

## 四、P1：高优先级问题

### P1-1 Premiere UXP Bridge 当前无法完成一键排片

涉及文件：

- `uxp-bridge/main.js:98-157`
- `uxp-bridge/manifest.json:6-13`

问题说明：

Bridge 使用了与当前 Premiere UXP API 不一致的调用：

- `require("premiere").app`
- `app.project`
- `project.importFile()`
- `project.createNewSequence()`
- `seq.addMarker()`

当前 Premiere UXP DOM 使用 `require("premierepro")`，活动项目通过 `app.Project.getActiveProject()` 获取。素材导入、序列创建、标记写入也需要使用当前 API 和 Action 事务。

此外，即使上述 API 全部能运行，当前 Bridge 也只导入素材、创建序列和尝试写标记，没有把视频素材按照清单顺序插入 V1 轨道。

Manifest 中最低版本写为 Premiere 22.0，但当前 UXP DOM 实际应按支持的 Premiere 25.x 版本开发和测试。

修复建议：

1. 暂时将 Bridge 标记为实验性，稳定版优先交付 FCP7 XML。
2. 按当前 Adobe UXP API 重写项目获取、素材箱、素材导入、序列创建和标记动作。
3. 项目清单必须包含稳定镜头 ID、素材文件名或素材绝对路径映射。
4. 导入后建立 `takeId -> ProjectItem` 映射。
5. 使用 SequenceEditor 按时间位置插入 V1/A1 等轨道。
6. 加入重复导入的幂等处理，避免重复素材箱和重复序列。

验收标准：

- 在真实 Premiere 中成功加载插件。
- 导入 3 个测试片段后，序列中按 MOMO 顺序出现 3 个视频片段。
- 音频、标记、片段时长和起始时间正确。
- 重复执行不会无限创建重复素材箱。

参考：

- Adobe Premiere UXP DOM：<https://developer.adobe.com/premiere-pro/uxp/ppro_reference/>
- Adobe Premiere Project API：<https://developer.adobe.com/premiere-pro/uxp/ppro-reference/classes/project>

---

### P1-2 时间线存在两份数据，界面和导出结果可能不一致

涉及文件：

- `src/modules/director/EditingPage.tsx:24-32`
- `src/core/directorExport.ts:127-138`
- `src/core/directorEngine.ts:271-314`
- `src/modules/director/GenerationPage.tsx:193-207`
- `src/modules/director/ScriptPage.tsx:42`

问题说明：

剪辑页展示的时间线根据 `scenes + approvedTakeId` 实时计算，而 XML、SRT、项目清单等导出使用持久化的 `project.timeline`。

目前只有 `approveTake()` 会调用 `rebuildTimeline()`。以下操作不会同步时间线：

- 重新拆分剧本。
- 删除已采用 Take。
- 修改片段时长。
- 对白优先策略调整时长。
- 其他直接更新 `scenes` 的操作。

影响：

- 界面显示的新时间线和 XML 导出的旧时间线不一致。
- XML 可能引用已经删除的 Take。
- 导出时长可能仍是调整前的时长。

修复建议：

优先方案：不持久化 `timeline`，将它作为派生数据，在预览、XML、SRT、清单导出时统一计算。

备选方案：所有场景、片段和 Take 修改必须经过统一事务函数，并自动重建时间线。

验收标准：

- 删除采用 Take 后，预览、XML、SRT 和项目清单同时更新。
- 调整片段时长后，所有导出格式使用新时长。
- 项目中不存在引用已删除 Take 的时间线条目。

---

### P1-3 一次导入多个 Take 时只会保留最后一个

涉及文件：

- `src/modules/director/GenerationPage.tsx:155-172`

问题说明：

文件循环内每次都基于组件闭包中的旧 `project.scenes` 创建新数组。多文件导入时，后一次 `updateProject()` 会覆盖前一次追加。

修复建议：

- 先转换并收录所有文件，再一次性追加全部 Take。
- 或者每轮从 `useDirector.getState()` 获取最新项目。

验收标准：

- 一次选择 5 个视频文件后，片段中新增 5 个 Take，而不是只有最后一个。

---

### P1-4 音频导演台的场景和片段绑定尚未实现

涉及文件：

- `src/modules/director/EditingPage.tsx:287-380`
- `src/core/directorExport.ts:37-117`
- `src/core/directorExport.ts:328-348`

问题说明：

界面描述为：

- 对白/旁白绑定镜头。
- 音效绑定剧情事件。
- 环境音/音乐绑定场景。

但新增音频轨道时没有传 `segmentId` 或 `sceneId`。结果是：

- 对白优先策略找不到音轨。
- 画面优先策略找不到音轨。
- SRT 无法生成对应字幕。
- Premiere 音频位置只能使用固定时长或列表顺序猜测。

同时，类型中虽然有声线、语速、情绪、多版本等字段，界面没有真正提供编辑入口。

修复建议：

1. 新建音频时选择绑定对象。
2. 对白/旁白选择片段或镜头。
3. 环境音/音乐选择场景和有效区间。
4. 提供 voice、speed、emotion、音量、淡入淡出设置。
5. 音频 Take 增加采用版本，避免只有一个 `assetId`。

验收标准：

- 一条对白绑定片段后能够生成 SRT。
- 对白优先策略能根据真实音频时长调整对应片段。
- XML 中的音频出现在对应视频片段的时间位置。

---

### P1-5 “导出为节点成片”重启后会失效

涉及文件：

- `src/modules/director/EditingPage.tsx:136-159`
- `src/core/videoEdit.ts:150-182`

问题说明：

`concatVideos()` 返回临时 `blob:` URL。剪辑页把它直接写入导演台节点，没有进入资产库，也没有写入 `project.exportAssetId`。

关闭软件后 `blob:` 地址失效，节点将无法再次播放或向下游传递成片。

修复建议：

1. 拼接完成后调用资产库 `collect()`。
2. 将临时 Blob 落盘为稳定视频资产。
3. 节点写入稳定资产 URL。
4. 项目记录 `exportAssetId`。
5. 新导出覆盖旧导出时考虑是否保留历史版本。

验收标准：

- 导出成片后重启 MOMO，导演台节点仍可播放成片。
- 成片能在资产库找到。
- 成片可以继续连接到其他视频节点。

---

### P1-6 批量取消会留下永久排队任务

涉及文件：

- `src/core/directorQueue.ts:212-217`
- `src/core/directorQueue.ts:262-305`

问题说明：

批量执行开始时会提前创建所有 Take。取消只会在任务之间跳出循环，没有：

- 取消当前网络请求。
- 取消 ComfyUI 轮询。
- 把未开始任务从 `queued` 改为 `cancelled`。
- 返回取消数量。
- 区分“完成”和“取消”。

因此未运行的 Take 会永久停留在排队中；当前远程请求还可能继续计费。

修复建议：

1. 将 AbortSignal 传入远程生成、ComfyUI 轮询和下载流程。
2. 未开始任务统一标记 `cancelled`。
3. 当前不可撤销的服务端任务明确显示“取消等待，但服务端可能继续执行”。
4. `runBatch()` 返回 `{done, failed, cancelled}`。
5. 取消后不要发送 100% 完成进度。

验收标准：

- 批量 5 个任务，在第 2 个任务时取消，剩余任务全部显示“已取消”。
- 取消后没有永久 `queued` Take。
- UI 明确显示“已取消”，而不是“批量完成”。

---

### P1-7 Ollama GGUF Modelfile 会生成无效内容

涉及文件：

- `src/core/services/ollama.ts:241-256`
- `src/core/services/ollama.ts:264-272`

问题说明：

当前生成内容包含：

- `PROJECTOR <mmproj>`
- `PARAMETER num_gpu`
- 未转义的 GGUF 文件路径
- `think=false` 时只生成注释，没有实际关闭 thinking

Ollama 当前官方 Modelfile 指令中没有 `PROJECTOR`，官方列出的合法参数中也没有 `num_gpu`。用户的模型路径中还可能包含空格，例如 `G:\ciomfyui AI\...`，直接拼接容易导致解析失败。

修复建议：

1. 普通文本 GGUF 只生成官方支持的 `FROM`、`PARAMETER num_ctx`、`SYSTEM` 等内容。
2. 路径包含空格时正确引用，或建议将 Modelfile 放到 GGUF 同目录并使用相对路径。
3. mmproj 多模态导入标记为实验性，不生成未经验证的 `PROJECTOR` 指令。
4. 必要时提供 llama.cpp 服务配置方案。
5. 限制模型名字符，避免生成危险或无法执行的终端命令。
6. `think` 应通过 Ollama API 的能力参数或模型确认支持的方式控制，不应只有注释。

验收标准：

- 使用含空格的 Windows 路径生成 Modelfile 后，`ollama create` 可以成功执行。
- 普通 Qwen/DeepSeek GGUF 可以正常对话。
- 不支持的视觉 GGUF 明确提示替代方案，而不是生成错误配置。

参考：

- Ollama Modelfile：<https://docs.ollama.com/modelfile>
- Ollama GGUF 导入：<https://docs.ollama.com/import>

## 五、P2：功能完整性与一致性问题

### P2-1 `.momoskill` 界面显示支持，实际会拒绝

涉及文件：

- `src/modules/skills/SkillManager.tsx:51-65`
- `src/core/skillImport.ts:111-202`

问题说明：

核心层已经实现 `importSkillPackage()`，但界面对 `.momoskill` 和 ZIP 直接提示“后续版本支持”。文件选择器和帮助文字却仍然写着支持 `.momoskill`。

修复建议：

- 方案 A：接入 JSZip，完成解压、安全限制、解析和引用文件持久化。
- 方案 B：当前版本只接受 `SKILL.md`，移除 `.momoskill/.zip` 的 accept 和支持文案。

补充注意：当前包解析器只保存了 references 文件名，没有保存内容，后续 Skill 执行仍无法读取参考资料。

---

### P2-2 手工创建的 ComfyUI 子工作流没有参数和语义槽

涉及文件：

- `src/modules/comfy/TemplateManager.tsx:500-510`
- `src/modules/comfy/TemplateManager.tsx:595-607`
- `src/core/services/comfy.ts:452-458`

问题说明：

手工新增分支时：

```ts
params: [],
slots: [],
```

后续面板只允许选择归属节点和输出节点，不能生成或修改参数与语义绑定。只要选择了这个分支，`effectiveParams()` 就会使用空的分支参数，不再回退模板顶层参数。

修复建议：

增加：

- 从选中节点生成公开参数。
- 自动识别语义槽。
- 手工设置节点 ID 和 input。
- 置信度和判断依据。
- 绑定校验。
- 低成本试跑。
- 工作流指纹变化后自动取消“已验证”。

---

### P2-3 `object_info` 精确输入识别路径不正确

涉及文件：

- `src/core/services/comfy.ts:186-200`

问题说明：

当前读取：

```ts
oi.inputs?.[slot.input]
```

但 ComfyUI `object_info` 的输入通常位于：

- `input.required`
- `input.optional`

项目后面的提交校验逻辑已经使用后一种结构，说明当前自动识别分支没有正确读取真实类型。

影响：

- 图片、视频、音频、数字和文本类型的“精确修正”基本不会生效。
- 最终仍依赖节点名称和编号猜测。

---

### P2-4 首帧、尾帧和多参考图推断仍然过度依赖节点顺序

涉及文件：

- `src/core/services/comfy.ts:157-175`

问题说明：

当前默认将第一个 LoadImage 当首帧，第二个根据标题判断尾帧，否则当参考图。节点 ID 通常只是数字，使用节点 ID 判断 `last/end` 基本无效。

在以下工作流中容易识别错误：

- 多参考角色图。
- 图生图参考图。
- 首尾帧工作流。
- ControlNet/姿势/深度输入。
- 同时有角色图和环境图的工作流。

建议：

- 以节点标题、下游拓扑、目标输入类型和 APP/子图声明共同打分。
- 中低置信度必须由用户确认。
- 不要仅按节点 ID 排序自动绑定。

---

### P2-5 剧本拆分的镜头时间仍可能越界

涉及文件：

- `src/core/directorEngine.ts:150-166`
- `src/core/directorEngine.ts:102-111`

问题说明：

当前镜头时间规整逻辑：

```ts
const start = Math.max(prevEnd, Math.min(dur - 1, sh.startSec ?? 0));
const end = Math.max(start + 1, Math.min(dur, sh.endSec ?? dur));
```

如果前一个镜头已经到达片段末尾，下一个镜头可能得到：

- `start >= durationSec`
- `end = start + 1`
- `end > durationSec`

后续只裁 `endSec`，可能又形成 `endSec < startSec`。

修复建议：

- 无法容纳的新镜头直接过滤或合并。
- 或按照镜头数量重新分配时间段。
- 最终增加不变量校验：`0 <= start < end <= durationSec`。

---

### P2-6 XML、SRT、CSV 保存对话框始终使用 JSON 类型

涉及文件：

- `src/modules/comfy/templateIO.ts:126-140`

问题说明：

`saveTextFile()` 无论保存 XML、SRT 还是 CSV，都使用：

```ts
filters: [{ name: "JSON", extensions: ["json"] }]
```

浏览器下载的 Blob MIME 也固定为 `application/json`。

修复建议：

根据文件扩展名自动选择：

- `.xml` → `application/xml`
- `.srt` → `application/x-subrip` 或 `text/plain`
- `.csv` → `text/csv`
- `.json` → `application/json`

---

### P2-7 Premiere XML 使用了原始 Windows 路径

涉及文件：

- `src/core/directorExport.ts:151-186`
- `src/core/directorExport.ts:201-220`

问题说明：

FCP7 XML 的 `pathurl` 当前直接写入 Windows 文件路径。Premiere/FCP XML 通常需要 `file://localhost/...` 形式并进行 URL 编码。

建议增加统一函数：

```ts
function toFileUrl(path: string): string
```

并用真实中文路径、空格路径、盘符路径进行 Premiere 导入测试。

---

### P2-8 导演台节点删除、撤销和重做存在项目归档竞态

涉及文件：

- `src/core/stores/boardStore.ts:739-783`
- `src/core/stores/boardStore.ts:1207-1237`

问题说明：

导演台项目归档使用异步动态导入。用户删除节点后立即撤销时，可能先执行恢复，随后异步归档才完成，导致节点已经恢复但项目又被移动到归档区。

同时 `redo()` 直接应用节点快照，不会调用 `removeNode()`，注释中所说的“redo 删除由 removeNode 触发”并不成立。

修复建议：

- 将节点快照和导演台项目状态放到同一个事务中。
- Undo/redo 应比较前后 Director 节点集合，统一执行项目归档/恢复。
- 避免从 boardStore 直接绕过 directorStore 的持久化序号守卫写文件。

## 六、README 路线图状态需要校正

涉及文件：

- `README.md:152` 之后的“导演台与本地创作编排”章节。

当前很多只有数据结构、UI 骨架或部分逻辑的功能已经标记为 `[x]`，例如：

- 模板绑定向导。
- `.momoskill` 安全导入。
- 模型无关生成配方。
- 批量任务队列。
- 音频导演台。
- Premiere Bridge。
- 完整交付包。
- 3D 模型导入与 ComfyUI 语义映射。

建议使用三态：

- `[x]`：已经完成并通过端到端验证。
- `[~]`：数据结构/UI/实验性骨架完成。
- `[ ]`：尚未实现或真实链路未接通。

否则下一轮 AI 编程工具会根据 README 错误判断功能已完成，从而跳过关键调用链。

## 七、建议增加的工程保障

### 1. 生成前预检

批量生成前统一展示：

- 任务数量。
- 本地/远程任务数量。
- 使用的模型和配方。
- 预计调用次数。
- 缺失语义槽。
- 首尾帧和参考图数量。
- 不兼容能力。
- 可能产生费用的任务。

预检通过后再提交。

### 2. 导演台最小自动测试集

建议引入 Vitest，至少覆盖：

1. ComfyUI 运行不改变原模板。
2. 子工作流只提交本分支和依赖闭包。
3. 语义槽写入准确。
4. 多参考图不会覆盖同一个 binding。
5. 多文件导入保留全部 Take。
6. 时间线与 scenes/approvedTakeId 一致。
7. 删除 Take 后导出不引用旧 Take。
8. 批量取消后无永久 queued Take。
9. Shot 满足 `0 <= start < end <= durationSec`。
10. XML 路径可以被 Premiere 识别。

### 3. 端到端测试样例

至少保存四套不含真实密钥的测试工作流和样例：

- SeedVR2 图片放大分支。
- SeedVR2 视频放大分支。
- MiniMax H3 首尾帧或多参考工作流。
- Seedance 中转站远程视频配方。

每套测试应覆盖：导入、自动识别、人工确认、生成、资产落盘、重启恢复、导出。

### 4. Director/Skill 数据迁移

导演台和 Skill 都是长期持久化数据，建议像 Settings 一样建立明确的 schema 迁移：

- `schemaVersion`
- `normalizeDirectorProject()`
- `normalizeSkill()`
- 旧字段补默认值
- 未知字段宽容保留或明确丢弃
- 迁移失败进入保护模式，不覆盖原文件

### 5. 真实交付回归

每次发布前至少执行：

- 重启 MOMO 后所有采用 Take 可播放。
- 导出成片节点重启后仍有效。
- Premiere 能导入 XML，素材不离线。
- 中文路径、空格路径可以导入。
- SRT 时间码与视频顺序一致。
- 删除、撤销、重做导演台节点后项目不丢失。

## 八、建议修复顺序

### 第一阶段：先保证生成内容正确

- [ ] 修复 ComfyUI 模板原地修改。
- [ ] 接通导演台语义槽。
- [ ] 完成生成配方编辑和片段分配。
- [ ] 修复 `object_info` 类型读取。
- [ ] 修复首帧/尾帧/多参考绑定确认流程。

### 第二阶段：保证任务和资产不会丢

- [ ] 时间线改为统一派生数据。
- [ ] 修复批量取消状态机。
- [ ] 修复多文件 Take 导入。
- [ ] 成片落资产库并持久化。
- [ ] 修复导演台节点撤销/重做归档事务。

### 第三阶段：完成音频和交付

- [ ] 实现音频场景/片段绑定。
- [ ] 修复 XML 文件 URL。
- [ ] 修复 XML/SRT/CSV 保存类型。
- [ ] 使用真实 Premiere 验证 FCP7 XML。
- [ ] 重写或暂时隐藏 UXP Bridge。

### 第四阶段：完善本地模型和扩展系统

- [ ] 修复 Ollama Modelfile。
- [ ] 完成或收回 `.momoskill` 支持声明。
- [ ] 完成 Skill references 内容持久化和使用。
- [ ] 增加 Director/Skill schema 迁移。
- [ ] 校正 README 路线图状态。

## 九、发布门槛建议

满足以下条件后再考虑标记为稳定版本：

- P0 问题全部关闭。
- P1 问题全部关闭或明确降级隐藏。
- 真实 ComfyUI 完成至少两套多分支工作流验证。
- 真实远程视频模型完成至少一套批量生成验证。
- 成片资产重启后可继续使用。
- Premiere XML 实机导入成功。
- 取消、失败、重试、重启恢复均经过测试。
- README 不再把实验性骨架标记为已完成。

## 十、项目优势判断

这些问题修复后，MOMO 的主要优势仍然非常明确：

1. 不复制 ComfyUI，而是将复杂工作流封装为可理解的创作入口。
2. 不复制 Premiere，而是负责从剧本到可剪辑素材的完整上游链路。
3. 本地 ComfyUI、Ollama 和远程视频模型可以统一进入同一个项目与 Take 管理体系。
4. 剧情缺片、版本确认、提示词规则、资产落盘和剪辑交付可以在一个工作台完成。
5. Skill 能够跨导演台、提示词、海报和其他创作节点复用。

当前最重要的工作不是继续增加功能，而是把已经存在的数据结构、界面和执行函数连接成一条真正可靠、可追溯、重启后不丢失的生成链路。
