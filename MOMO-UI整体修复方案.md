# MOMO 智能画布 UI 整体修复方案

## 1. 文档用途

本文档用于指导后续 AI 或开发人员修复 MOMO 智能画布的 UI 问题。

本轮修复以统一视觉、调整信息层级、优化导演台和改善交互可读性为主，不重写现有业务逻辑，不改变画布工作流、模型调用、ComfyUI 调用、Skill、导演台数据结构和持久化行为。

> **2026-08 补充**：对全部 20 个画布节点逐一审查后，新增「7.5 节点本体 UI 规范」「第零阶段：令牌级扫荡与节点修复」「18.4 节点验收」「18.5 可执行检查」和「附录 A 节点问题清单」。原方案第 7 章只覆盖画布外围（顶部栏/工具栏/节点坞），而审查发现最系统的问题在节点内部；另有若干混在样式债里的功能性 bug（失效令牌、死 class、悬停隐形的徽章），一并列入第零阶段先修。

## 2. 修复目标

1. 让画布、导演台、设置、资产库、角色库、创作助手、ComfyUI 模板管理器使用统一的视觉语言。
2. 将导演台从表单式页面调整为模块化创作工作台。
3. 主要功能按钮统一使用“图标 + 文字”，减少难以理解的纯图标按钮。
4. 统一圆角、间距、字号、卡片、状态和操作层级。
5. 取消设置页面中的多层弹窗嵌套。
6. 提高深色主题下的文字对比度和可读性。
7. 保证云白、深空蓝、深邃黑三个主题均正常显示。
8. 保持桌面软件的信息密度，不将软件改成移动端大卡片风格。

## 3. 修改边界

### 3.1 本次允许修改

- UI 组件结构和页面布局。
- CSS、主题令牌和响应式样式。
- 按钮文案、页面标题、帮助说明和状态文案。
- 设置页内部导航结构。
- 导演台各业务模块的展示方式。
- 弹窗、抽屉、空状态、卡片和状态标签。
- 键盘焦点、Esc 关闭、滚动区域和可访问性属性。

### 3.2 本次不得修改

- 不修改节点运行逻辑和 `runner.ts` 的业务行为。
- 不修改模型请求协议和服务层行为。
- 不删除导演台、Skill、ComfyUI、本地模型、音频或导出功能。
- 不把导演台改成视频剪辑软件。
- 不在软件内部重新实现完整 ComfyUI。
- 不引入第三方图标库，继续使用 `src/ui/icons.tsx`。
- 不硬编码主题颜色，继续使用现有主题变量。
- 不破坏浏览器预览模式。
- 不修改用户已有数据结构；如确实必须修改设置结构，需要补充迁移。

## 4. 全局 UI 规范

### 4.1 圆角

统一使用现有主题令牌：

- 工作区主卡片、主要弹窗：`var(--r-lg)`，默认 16px。
- 普通模块、输入框、列表项：`var(--r-md)`，默认 12px。
- 紧凑按钮和小控件：`var(--r-sm)`，默认 9px。
- 状态标签、筛选标签：`var(--r-full)`。

禁止在同一页面随意混用 4px、6px、7px、8px、10px 等独立圆角。只有缩略图内部标记等特殊小元素可以例外。

### 4.2 间距

使用 4px/8px 基础间距体系：

- 控件内部小间距：4px、8px。
- 同一模块内元素间距：8px、12px。
- 卡片内边距：16px。
- 模块之间间距：16px、24px。
- 页面边距：24px；窗口较小时降为 16px。

### 4.3 字号

- 页面标题：18px。
- 模块标题：14px 至 16px。
- 正文和主要控件：13px 至 15px。
- 辅助说明：不低于 12.5px。
- 状态标签：不低于 12px。
- 禁止大面积使用 10px、11px 字号。

### 4.4 按钮

按钮分为四类：

1. `primary`：当前区域唯一的主要下一步操作。
2. `secondary`：普通操作。
3. `danger`：删除、移除、取消任务等危险操作。
4. `icon-only`：仅用于关闭、更多、缩放、撤销、重做、星标等通用动作。

以下操作必须使用“图标 + 文字”：

- 导入、上传、生成、拆分、分析、保存、导出。
- 采用版本、重新生成、修改提示词。
- 打开管理器、批量处理、测试连接。
- 删除配方、删除实体等不应误触的操作。

纯图标按钮必须具备 `aria-label` 和 `title`，并提供明确的悬停和键盘焦点状态。

### 4.5 卡片

新增统一的模块卡片结构：

```text
模块卡片
├─ 头部：图标 + 标题 + 简短说明 + 状态 + 更多操作
├─ 内容：摘要、预览、表单或列表
└─ 底部：次要操作 + 主要操作
```

卡片状态包括：

- 默认。
- 悬停。
- 选中。
- 运行中。
- 已完成。
- 警告。
- 错误。
- 禁用。

卡片选中和运行状态使用 `color-mix()` 基于 `var(--accent)` 生成，不硬编码蓝色光晕。

### 4.6 文字颜色

- `var(--text-1)`：标题、主要内容和重要数据。
- `var(--text-2)`：普通说明和次要内容。
- `var(--text-3)`：占位、时间、非关键辅助信息。

深色主题中的 `text-3` 当前偏暗，应适当提高亮度。禁止用 `text-3` 显示操作说明、关键状态或较长正文。

### 4.7 动效和焦点

- 为所有按钮、链接、输入框、页签和可点击卡片增加可见的 `:focus-visible`。
- 弹窗打开后焦点进入弹窗，关闭后返回触发按钮。
- 弹窗支持 Esc 关闭。
- 弹窗打开时焦点不能进入背后页面。
- 支持 `prefers-reduced-motion: reduce`，关闭不必要的缩放和淡入动画。

## 5. 建议新增的共享组件

在 `src/ui/` 中增加或扩展以下组件，避免每个模块继续自行编写结构和内联样式：

- `WorkspaceShell`：全屏工作区外壳。
- `WorkspaceHeader`：项目名称、状态、全局操作。
- `SideNavigation`：工作区左侧流程导航。
- `ModuleCard`：统一模块卡片。
- `ModuleHeader`：图标、标题、说明和状态。
- `ActionButton`：图标加文字按钮。
- `IconAction`：受限制的纯图标按钮。
- `StatusBadge`：成功、警告、错误、运行中、缺失等状态。
- `EmptyState`：统一空状态。
- `InspectorPanel`：右侧选中项属性面板。
- `ProgressSummary`：项目完成情况摘要。
- `BatchActionBar`：批量任务范围和运行状态。
- `MediaTakeCard`：图片或视频版本卡片。

逐步减少页面内的 `style={{ ... }}`，将可复用样式放入共享组件和 CSS 类中。

**建设节奏**：不搞一次性全量建设。`ModuleCard` / `StatusBadge` / `EmptyState` / `ActionButton` 四个先在样板节点与导演台用起来，验证形态后再推广；其余组件按需补齐，避免组件先行的 big bang 烂尾。

## 6. 导演台整体重构

### 6.1 定位

导演台不是剪辑软件。它负责串联以下流程：

```text
剧本导入 → 分段 → 分镜 → 提示词与规则 → 批量生成 → 版本选择 → 缺失检查 → 连续性检查 → 交付剪辑
```

用户进入导演台后，无需逐页查找，就应立即看见：

- 项目目标时长。
- 场景、片段和镜头数量。
- 已生成、已采用、缺失和失败数量。
- 当前正在执行的任务。
- 连续性问题。
- 是否可以导出。

### 6.2 总体布局

将当前顶部页签加单列内容的布局改为：

```text
┌────────────────────────────────────────────┐
│ 项目栏：名称｜画幅｜目标时长｜总体进度｜主要操作 │
├──────────┬────────────────────┬─────────────┤
│ 左侧流程  │ 中间模块化工作区      │ 右侧属性检查器 │
│          │                    │             │
│ 剧本整理  │ 当前阶段相关模块      │ 当前选中场景   │
│ 分镜规划  │                    │ 片段或 Take    │
│ 批量生成  │                    │ 的详细参数     │
│ 成片检查  │                    │             │
│ 站位参考  │                    │             │
└──────────┴────────────────────┴─────────────┘
```

右侧没有选中内容时可以收起，为中间工作区释放空间。

### 6.3 导航名称

将导演台导航调整为：

- 剧本整理。
- 分镜规划。
- 批量生成。
- 成片检查。
- 站位参考。

将当前“剪辑”改名为“成片检查”或“检查与交付”，避免让用户误以为软件内置完整剪辑器。

### 6.4 项目栏

项目栏固定在导演台顶部，包括：

- 返回画布。
- 项目名称。
- 画幅。
- 目标时长。
- 自动保存状态。
- 总体完成进度。
- 当前主要操作。
- 更多操作。

关闭按钮可以保留纯图标，但必须有 `aria-label`。

### 6.5 项目概览模块

在导演台默认区域增加项目概览，显示：

- 场景数。
- 片段数。
- 镜头数。
- 已生成数量。
- 已采用数量。
- 缺失数量。
- 失败数量。
- 当前总时长和目标时长差。

点击任意统计项后，跳转到对应筛选结果。

### 6.6 剧本导入与拆分模块

将剧本文本、分段检测和拆分操作收进同一个模块。

主要按钮：

- `导入剧本`。
- `识别分段`。
- `AI 拆分`。
- `查看拆分结果`。

支持粘贴、文件导入和外部 AI 已分段文本。自动识别“分段1、分段2、分段3”、自定义分隔符和标题格式。

识别结果必须先预览，不直接覆盖原剧本。

### 6.7 全局规则与 Skill 模块

将以下内容整合到一个“全局创作规则”模块：

- 统一风格。
- 场景定调。
- 角色连续性。
- 画面规则。
- 负向规则。
- 已启用 Skill。
- 提示词范例和配方。

显示规则应用范围：

- 整个项目。
- 指定场景。
- 指定片段。
- 指定镜头。

局部规则覆盖全局规则时，需要明确显示“已覆盖全局设置”。

### 6.8 场景、片段和镜头

采用三层结构：

```text
场景卡
└─ 片段卡
   └─ 镜头卡
```

场景卡显示：

- 场景名称和地点。
- 场景时长。
- 片段数量。
- 角色。
- 场景状态。
- 缺失数量。

片段卡显示：

- 片段编号和摘要。
- 目标时长。
- 镜头数量。
- 提示词状态。
- 生成状态。
- Take 数量。
- 是否已采用。
- 是否锁定。

镜头卡显示：

- 镜头编号。
- 起止时间。
- 景别。
- 镜头运动。
- 人物动作。
- 音频提示。
- 承接关系。

不要将镜头全部压缩为一行 11px 小字。

### 6.9 批量生成模块

将多个并列按钮收拢为统一批量操作栏。

主要操作：`开始批量生成`。

生成范围通过下拉菜单或分段选择器设置：

- 全部片段。
- 缺失片段。
- 已修改片段。
- 失败片段。
- 当前选择。

队列必须显示：

- 等待数量。
- 生成中数量。
- 完成数量。
- 失败数量。
- 当前任务。
- 总体进度。

提供暂停、继续、取消和重试失败项，不把这些按钮全部长期平铺显示。

### 6.10 生成配方

生成配方使用独立模块或右侧抽屉编辑，不在主列表中展开大量表单。

配方卡显示：

- 配方名称。
- 本地 ComfyUI 或远程 API。
- 图片或视频。
- 文生、图生、首尾帧或多参考模式。
- 当前模板或模型。
- 是否可用。

编辑配方时再展示完整参数。

### 6.11 Take 版本卡

Take 必须使用图片缩略图或视频封面，不使用纯文字卡片。

卡片显示：

- 预览图。
- 版本编号。
- 图片或视频。
- 使用的配方和模型。
- 生成时间。
- 状态。
- 是否星标。
- 是否采用。

操作按钮：

- `预览`。
- `采用此版本`。
- `修改提示词`。
- `重新生成`。
- `删除`。

除星标和更多菜单外，以上操作使用图标加文字。

### 6.12 成片检查

包含以下模块：

- 缺片检查。
- 故事顺序。
- 总时长检查。
- 角色连续性。
- 场景连续性。
- 动作和镜头承接。
- 音频覆盖。
- 交付状态。

每个问题可以点击并定位到具体场景、片段或镜头。

导出操作分组显示：

- `导出 Premiere 项目`。
- `导出剪映项目`。
- `导出镜头表`。
- `导出 SRT`。
- `导出项目清单`。
- `输出到画布节点`。

主界面只保留一个主要导出按钮，其他格式进入下拉菜单或导出面板。

### 6.13 音频导演台

音频导演台作为成片检查中的独立模块，先实现轻量功能：

- 对白。
- 旁白。
- 环境音。
- 音效。
- 音乐建议。

每条音频绑定场景、片段或镜头，并显示：

- 类型。
- 文本或描述。
- 绑定位置。
- 是否已有素材。
- 是否已生成。
- 时长。

导演台只负责素材组织、生成、缺失检查和导出，不实现复杂混音时间线。

### 6.14 3D 站位参考

保留俯视图工作方式，重新组织为：

- 中央站位画布。
- 左侧实体列表。
- 右侧属性检查器。
- 底部导出操作。

支持显示：

- 角色位置。
- 角色朝向。
- 摄像机位置和朝向。
- 行动路径。
- 前景、中景和背景区域。
- 景深参考。

导出按钮：

- `导出站位图`。
- `导出深度图`。
- `导出分区图`。
- `添加到当前镜头`。

## 7. 画布 UI 修复

### 7.1 顶部栏

- 保留画布标签和全局状态。
- 创作助手、资产库、角色库等业务入口在空间足够时显示图标加文字。
- 窗口较窄时可收缩为图标，但必须有提示和可访问名称。
- 将低频入口放入“更多”，避免右侧连续堆放大量同权重图标。

### 7.2 左侧工具栏

- 保留紧凑图标布局。
- 选中工具时显示工具名称。
- 危险的“清空画布”与普通工具分隔。
- 锁定、忽略等状态需要明显的开启状态，而不只改变微弱颜色。

### 7.3 底部节点坞

将节点按以下类别分组：

- 输入：图片、视频、提示词、风格预设、备注。
- 规划：分镜、导演台、角色卡。
- 生成：生成图像、生成视频、ComfyUI。
- 处理：打光、多角度、超清放大、智能矢量、电商长图。

默认显示常用节点，其他节点进入“更多节点”。保留运行全部、撤销、重做和缩放区域，但与节点添加区域增加视觉分隔。

### 7.4 节点浮动操作条

- 常用操作保留在外层。
- 删除、复制、忽略等低频操作放入更多菜单，或在节点选中后显示。
- 输出模式等业务选择不能与删除按钮混成同一层级。

### 7.5 节点本体 UI 规范（2026-08 新增）

原方案只规定了画布外围，本章补齐节点内部的统一规范。审查发现各节点各自为战：运行态有 4 种写法、说明文字被单行截断、小字号成片。

#### 7.5.1 运行态展示统一

- 所有可运行节点的运行态统一为三件套：`progress-line`（阶段文案）+ 进度条 + 百分比，以 `ComfyNode.tsx:75-86` 为样板。
- 重新生成时必须保留旧结果（留旧图叠加运行态），骨架屏 `.skeleton` 仅用于首次空态。禁止运行中把已有成果替换成空白骨架（打光、多角度节点现状如此）。
- 导入中的媒体节点（VideoNode 等）running 时不得仍显示「点击导入」空态，应显示导入中骨架。

#### 7.5.2 信息分块，禁止大段文字

- 节点正文说明超过一行时，禁止塞进 `.gen-sum`（其为 nowrap + ellipsis，长文必被截断）；改为可换行的 hint 区块或「图标 + 短句」引导卡。角色卡节点 80+ 字说明、电商节点操作指引被截断是反面案例。
- 空态一律「图标 + 一行短句 + 主按钮」，不堆 60 字以上的产品说明（DirectorNode 空态为反面案例）。
- 参数与高级设置收进底部参数栏/浮层，节点本体只保留状态与主操作（StoryboardNode 在 360px 内堆 10+ 控件为反面案例）。
- 「卡片化」指信息分块加标题层级，不是加大留白；保持桌面端信息密度。

#### 7.5.3 可交互性

- 节点内一切可点控件必须有 `nodrag`：chips、色块、模式切换段、参考图胶囊、链接；textarea/滚动区另有 `nowheel`。
- 可点击区域（导入区 `.img-empty`、折叠头、来源链接）必须是 `<button>` 或带 `role="button" tabIndex={0}`，禁止裸 `div onClick`。
- 纯图标按钮必须有 `aria-label` + `title`。批量修复点：NodeShell 工具条、便签色点与删除钮、各发送按钮（`.gd-send` / `.send-btn`）。

#### 7.5.4 样式纪律（节点范围）

- 字号：正文与可点按钮 ≥12.5px；纯装饰角标（序号、字数）最低 11.5px；10px 级文字禁止出现在操作说明与按钮。
- 圆角只引 `--r-*` 令牌；`99px` 一律写 `var(--r-full)`。
- 光晕/描边一律 `color-mix(in srgb, var(--accent) N%, transparent)`。`base.css:93` `.btn.primary` 的硬编码蓝紫光晕是本项目反模式代表（黑主题橙按钮配蓝光晕）。
- 禁止内联 `style={{ ... }}` 布局。死内联 `acts style={{ opacity: 1 }}`（被 `.mnode-toolbar .acts { display: contents }` 拍平，纯无效）在 5 个节点复制粘贴，全部删除。
- 节点主图必须 `<Thumb>`；SVG 例外（VectorizeNode）需在代码注释中说明理由，防止被当范例扩散。

## 8. 设置 UI 修复

### 8.1 左侧导航

调整为：

- 模型与服务商。
- 本地模型。
- 协议。
- Skill。
- ComfyUI。
- 联网搜索。
- 素材与保存。
- 音频。
- 快捷键。
- 外观。
- 用量与稳定性。
- 关于与更新。

### 8.2 取消弹窗嵌套

以下功能不再从设置中打开第二层居中弹窗：

- Skill 管理。
- GGUF/本地模型管理。
- ComfyUI 模板管理器。
- 协议编辑器。

它们应成为设置内部子页面，或者使用右侧抽屉。需要返回时显示明确的返回按钮和页面标题。

### 8.3 模型配置

- 顶部不再平铺 Skill、GGUF、中转站预设和帮助按钮。
- Skill 移入左侧导航。
- “GGUF 导入”改名为“本地模型”。
- 中转站预设放入“添加服务商”的下拉选项。
- 默认模型摘要改为可点击的角色卡片。
- 空服务商状态缩小高度，并将“添加服务商”作为主要操作。

## 9. Skill 管理 UI 修复

### 9.1 类型区分

Skill 卡片显示类型：

- 提示词规则 Skill。
- 知识型 Skill。
- 自动化 Skill。

### 9.2 文案修正

如果当前版本只支持提示词规则，不使用“当前版本不执行脚本”作为主要提示。

改为：

> 导入后可在提示词工具、导演台和其他支持 Skill 的功能中选择并应用。当前支持提示词规则和知识内容；自动化脚本需要单独授权后运行。

如果尚未实现自动化 Skill，则改为：

> 当前支持提示词规则和知识内容，自动化脚本功能暂未开放。

### 9.3 Skill 卡片

显示：

- 名称。
- 版本。
- 类型。
- 描述。
- 适用场景。
- 权限。
- 启用状态。
- 最近使用时间。

提供 `查看详情`、`启用/停用`、`删除`。收藏可以保留纯图标。

## 10. ComfyUI UI 修复

### 10.1 设置页结构

分为两个主模块：

1. ComfyUI 连接。
2. 工作流模板。

连接模块显示地址、连接状态、测试连接和最近一次检测时间。

模板模块显示模板数量、可用数量、异常数量和主要操作。

### 10.2 智能模板向导

工作流导入采用四步向导：

1. 导入工作流 JSON。
2. 自动识别输入、参数和输出节点。
3. 用户确认节点绑定和逻辑分组。
4. 保存模板及二级子菜单。

自动识别结果按类型显示：

- 图片输入。
- 视频输入。
- 正向提示词。
- 负向提示词。
- 种子。
- 尺寸、帧数和采样参数。
- 图片输出。
- 视频输出。

系统不确定的节点必须标记“需要确认”，不能直接保存为已确认配置。

### 10.3 工作流逻辑分组

- 同一 JSON 中识别到多套工作流时，显示为独立模块块。
- 支持框选节点并创建逻辑分组。
- 每个逻辑分组可以设置名称、颜色和是否参与生成。
- 支持“仅运行此分组”。
- 主模板悬停或点击后显示二级子菜单。
- 颜色不能作为唯一识别方式，必须同时显示名称和状态。

## 11. 创作助手 UI 修复

- 第一行只保留“聊天 / Agent”模式切换和面板关闭。
- 联网搜索和语音入口移动到输入区附近。
- 清空对话放入更多菜单。
- 三个模型选择折叠为“本次使用模型”区域。
- 默认只显示当前对话模型摘要，需要时展开绘画和视频模型。
- 快捷提示卡降低高度，避免占用主要对话区域。
- 输入框提高文字和占位符对比度。
- 发送按钮在不可用时保持明确禁用状态。

## 12. 资产库 UI 修复

- 空状态中央增加 `导入素材` 主按钮。
- 搜索、筛选和批量操作分层，不放在同一行竞争。
- 未进入批量模式时隐藏全选和批量操作。
- 右下角含义不明确的纯图标改为图标加文字，或移入更多菜单。
- 文件夹和标签列表的编辑、删除操作仅在悬停或选中时显示。
- 素材详情使用右侧面板，不重复打开新弹窗。

## 13. 角色库 UI 修复

- 去除底部隐藏式横向滚动条。
- 角色列表使用自适应网格或带明确左右按钮的横向列表。
- 页面分为角色预览、角色信息和应用操作三个区域。
- “将产出”折叠为素材包模块。
- 标签过多时只显示主要标签，其余进入详情。
- 降低主要按钮的光晕强度。
- 窗口较小时角色详情允许纵向滚动，不裁切底部内容。

## 14. 弹窗和页面层级

- 禁止居中弹窗再次打开居中弹窗。
- 设置内部管理功能使用子页面或抽屉。
- 资产详情、配方编辑、片段属性等使用右侧抽屉。
- 需要用户确认的短操作使用确认弹窗。
- 全屏工作区使用 `WorkspaceShell`，不使用普通 Modal。
- 所有弹窗需要标题、关闭按钮、Esc 支持、焦点管理和最大高度限制。

## 15. 响应式要求

至少检查以下窗口宽度：

- 1920px。
- 1440px。
- 1280px。
- 1024px。

要求：

- 不出现页面整体横向滚动。
- 导演台右侧检查器在 1280px 以下可收起。
- 1024px 下导演台左侧导航缩成图标加短文字。
- 设置页面内容不被左侧导航挤压裁切。
- 角色库和资产库不出现无法操作的隐藏内容。
- 底部节点坞空间不足时自动折叠到“更多”。

## 16. 三主题检查

所有修复必须在以下主题验证：

- 云白。
- 深空蓝。
- 深邃黑。

重点检查：

- 文字对比度。
- 卡片边界。
- 选中状态。
- 强调色光晕。
- 输入框背景。
- 危险、警告和成功状态。
- 缩略图占位背景。

禁止在深邃黑主题出现橙色边框配蓝色硬编码光晕。

## 17. 实施顺序

### 第零阶段：令牌级扫荡与节点修复（2026-08 新增，先于第一阶段）

修功能性 bug 与系统性样式债，全部是低风险机械修改，见效最快：

1. 修复失效令牌：`canvas.css` 中 `--warning`（令牌实为 `--warn`）与 `--border`（全项目未定义却被引用十余处）——在 `theme.css` 补定义或全局替换为已存在的令牌。
2. 光晕令牌化：`base.css:93` `.btn.primary`、`canvas.css:1046` `.mnode-err`、多角度相机标记等硬编码 rgba 全部改 `color-mix`。
3. 修复死 class：`GenConfigPanel.tsx` 的 `.poplayer-overlay` 无 CSS 定义导致弹窗点击外部关闭失效，改用 `PopLayer`。
4. 修复 `DirtyBadge` 悬停隐形：移出 `.mnode-head` 或 hover 时保持可见。
5. 字号/圆角扫荡：`canvas.css` 内 10–12px 说明与按钮文字抬到 12.5px；5/6/7/8/10/14/20px 字面量圆角归并令牌。
6. `aria-label` / `nodrag` / 死内联三件套补齐（清单见附录 A）。
7. 被截断的长指引（角色卡、电商）改为可换行 hint；空态大段说明（导演台节点）改「图标 + 短句」。
8. 样板先行：以 `ImageGenNode` + `ComfyNode`（运行态样板）为参照，先改问题最多的 1–2 个节点（分镜、角色卡）验证规范，再推广到其余节点。

### 第一阶段：基础组件和全局样式

- 增加共享工作区、模块卡片、状态标签和检查器组件。
- 统一圆角、字号、间距和按钮。
- 增加焦点和减少动效支持。
- 提高低对比文字可读性。

### 第二阶段：导演台

- 重构工作区布局。
- 完成项目概览。
- 重构剧本、规则、分镜、批量生成和 Take。
- 重构成片检查、音频和 3D 站位。

### 第三阶段：设置、Skill 和 ComfyUI

- 整理左侧导航。
- 取消弹窗套弹窗。
- 重构本地模型、Skill 和模板管理页面。
- 实现 ComfyUI 智能识别向导的 UI。

### 第四阶段：画布和其他模块

- 整理顶部栏、左侧工具栏和底部节点坞。
- 修复创作助手、资产库和角色库。
- 完成响应式和三主题验证。

## 18. 验收标准

### 18.1 全局验收

- [ ] 主要业务操作全部为图标加文字。
- [ ] 纯图标按钮均有 `aria-label` 和 `title`。
- [ ] 页面没有大面积低于 12.5px 的正文。
- [ ] 设置中不存在弹窗套弹窗。
- [ ] 所有弹窗支持 Esc 和焦点管理。
- [ ] 1024px 宽度下没有页面整体横向滚动。
- [ ] 三个主题均无硬编码颜色冲突。
- [ ] 浏览器预览模式不会白屏。

### 18.2 导演台验收

- [ ] 进入导演台即可看到项目总体进度和缺失情况。
- [ ] 能从场景进入片段，再进入镜头或 Take。
- [ ] 片段状态能区分缺失、生成中、待选择、已采用和失败。
- [ ] 批量生成只保留一个主要按钮，并可选择执行范围。
- [ ] Take 显示缩略图或视频封面。
- [ ] 成片检查能定位具体问题片段。
- [ ] 音频与场景、片段或镜头存在明确绑定。
- [ ] 导演台没有完整剪辑软件式复杂时间线。
- [ ] Premiere、剪映和其他交付操作集中在交付模块。

### 18.3 ComfyUI 验收

- [ ] 导入工作流后能显示自动识别结果。
- [ ] 图片、视频、提示词、参数和输出节点分类明确。
- [ ] 不确定节点显示“需要确认”。
- [ ] 同一文件内的多套工作流能拆成独立逻辑块。
- [ ] 逻辑块支持名称、颜色、启用状态和仅运行此块。
- [ ] 模板可以形成主菜单和二级子菜单。

### 18.4 节点验收（2026-08 新增）

- [ ] 所有可运行节点运行态为 progress-line + 进度条 + 百分比。
- [ ] 重新生成时旧结果保留，骨架屏仅首次空态出现。
- [ ] 节点内无超过一行且被 nowrap 截断的说明文字。
- [ ] 节点内所有可点控件有 `nodrag`，导入区键盘可达。
- [ ] 纯图标按钮全部有 `aria-label` + `title`。
- [ ] 节点样式无 10px 级文字（装饰角标除外）、无字面量圆角、无内联布局 style。

### 18.5 可执行检查（2026-08 新增）

以下 grep 检查应全部通过（无输出即通过），可代替人工逐页核对：

```bash
# 不存在未定义令牌引用
grep -n "var(--warning)" src/styles/*.css          # 应为空
grep -n "var(--border)" src/styles/*.css           # 应为空（或已在 theme.css 定义）

# 无硬编码光晕
grep -n "rgba(110, 120, 255" src/styles/*.css      # 应为空
grep -n "rgba(91, 140, 255" src/styles/*.css       # 应为空

# 节点样式无 10px 级小字（命中的装饰角标需人工复核豁免）
grep -nE "font-size: (9|10|11)(\.5)?px" src/styles/canvas.css

# 无字面量圆角（应只剩豁免项）
grep -nE "border-radius: [0-9]+px" src/styles/canvas.css
```

## 19. 验证命令

每个阶段完成后执行：

```bash
npx tsc --noEmit
pnpm build
```

不要手动启动 `pnpm tauri dev`。不要执行 `pnpm tauri build`，除非用户明确要求打包。

## 20. 重点文件

- `src/styles/theme.css`
- `src/styles/base.css`
- `src/ui/kit.tsx`
- `src/ui/icons.tsx`
- `src/modules/director/DirectorStudio.tsx`
- `src/modules/director/director.css`
- `src/modules/director/ScriptPage.tsx`
- `src/modules/director/StoryboardPage.tsx`
- `src/modules/director/GenerationPage.tsx`
- `src/modules/director/EditingPage.tsx`
- `src/modules/director/ThreeDPage.tsx`
- `src/modules/settings/SettingsDialog.tsx`
- `src/modules/skills/SkillManager.tsx`
- `src/modules/comfy/TemplateManager.tsx`
- `src/modules/assets/AssetLibrary.tsx`
- `src/modules/assets/assets.css`
- `src/modules/agent/`
- `src/modules/charlib/`
- `src/modules/shell/shell.css`

## 附录 A：节点审查问题清单（2026-08，按文件）

20 个节点 + 外壳 + 参数面板逐一审查的结果，按「bug / 令牌 / 字号 / 圆角 / 无障碍 / 交互 / 信息层级 / 死代码」归类。行号以审查时为准，修复前先定位。

### A.1 功能性 bug（优先修）

| 位置 | 问题 |
| --- | --- |
| `canvas.css:887` | `.enh-est.warn b` 引用不存在的 `var(--warning)`（令牌为 `--warn`），警告文字静默不着色 |
| `canvas.css` 十余处（513/876/1726/4227/4280/4294…） | `var(--border)` 令牌全项目未定义；无回退处边框消失，有回退处深色主题近乎不可见 |
| `GenConfigPanel.tsx:172` | `.poplayer-overlay` 全库无 CSS 定义，「历次」弹窗点击外部关闭失效，应改用 `PopLayer` |
| `NodeShell.tsx:191` + `canvas.css:264-267` | `DirtyBadge` 位于悬停时 `opacity:0` 的 `.mnode-head` 内，「待更新」提醒在凑近操作时恰好消失 |
| `StoryboardNode.tsx:22` | 每次 board 任意变更都递归遍历全图上上游，应换扁平指纹 + `useShallow` |
| `VideoNode.tsx:37-46` | 导入中 `status:"running"` 但节点体仍渲染「点击导入视频」空态，无骨架 |
| `ComfyNode.tsx:107-109` | `results` 与 `videoResults` 同时存在时渲染两张 `.img-main`，未互斥 |
| `DirectorNode.tsx:69` | `&&`/`||` 无括号混排且 `data:video` 分支恒 false，封面判断逻辑需重写 |
| 全项目 8 处（`EnhanceLocalNode.tsx:30` 等） | 原生 `window.confirm` 与 Modal/PopLayer 体系割裂，中期换统一确认弹层 |

### A.2 硬编码颜色 / 光晕（黑主题破坏）

| 位置 | 问题 |
| --- | --- |
| `base.css:93` | `.btn.primary` 光晕 `rgba(110,120,255,.35)` 硬编码蓝紫，改 `color-mix(in srgb, var(--accent) 35%, transparent)` |
| `canvas.css:1046-1047` | `.mnode-err` 硬编码 `rgba(242,79,106,…)`，改 `color-mix` + `--danger` |
| `canvas.css:2444` | 多角度相机标记 `drop-shadow` 紫色硬编码，黑主题下图标为橙色 |
| `canvas.css:968/987/3427/3627-3642` | `.media-thumbs`/`.res-badge`/`.gd-ref`/`.gd-tag` 硬编码深色与 `#fff` 边框 |
| `canvas.css:2317` | `.note-dot` 边框 `rgba(0,0,0,.15)` 深色主题不可见，改 `color-mix` |
| `canvas.css:4155-4158` | `.hist-pop` 圆角 12px 字面量 + 硬编码阴影，改令牌 |
| `canvas.css:4114-4116` | `.gd-tier.on` 实色填充与全局 soft 底 on 态语言不一致 |

### A.3 字号失守（约定：说明 ≥12.5px）

- 参数弹卡：`.gp-hint` 10.5px（大面积）、`.gp-cell` 11px、`.gp-cell em` 9px、`.gp-seg button` 12px、`.gp-sec-title` 12px、`.gp-foot` 11.5px。
- 分镜节点：`.sb-cap` 11.5px、`.sb-tones button` 11px、`.sb-time` 10.5px、`.sb-shot .textarea` 12px、`.sb-line` 11.5px。
- 角色卡：`.cc-preset-tag` 10.5px、`.cc-name i` 12px、`.cc-meta` 12px、`.cc-chips .chip` 11.5px。
- 超清/矢量：`.enh-est` 10.5px、`.enh-pct` 11px、`.enh-report` 11px、`.enh-print label` 10px。
- 电商：`.ecom-mode-tag`/`.ecom-ref`/`.ecom-set-row .hint` 等 11–12px 成群。
- 其他：`.gen-sum` 12px、`.nt-btn` 12px、`.dirty-badge` 与 `.mnode.ign::after` 10.5px、`.src-chips a` 12px、`.gd-up-toggle` 11.5px、`.po-head` 11.5px。

### A.4 字面量圆角（应全部归并 `--r-*` 令牌）

`canvas.css`：`.gd-main`/`.gd-side-panel` 20px、`.ed-main` 18px、`.note-card` 14px、`.bubble` 14px+5px、`.think` 10px、`.chip` 99px、`.gd-ref` 10px、`.bimgs img`/`.thumbs img`/`.cc-deliv-imgs img`/`.up-row img` 8px、`.media-thumbs img`/`.thumb-ph` 7px、`.lang-seg button` 7px、`.swatch`/`.sb-time`/`.cc-palette i`/`.cc-check`/`.ecom-ref-item`/`.ecom-arrows button`/`.hist-thumb` 6px、`.ecom-card-ref` 5px、端口标签 6px 等约 30 处。

### A.5 无障碍（纯图标按钮缺 `aria-label`，均无 `title` 的单独标注）

- `NodeShell.tsx:56,203-215`：上游弹窗关闭、忽略/副本/删除（改一处全节点受益）。
- `NoteNode.tsx:20-24`：四个颜色圆点连 `title` 都没有；`:36` 删除钮无 `title`；`:28` 锁定钮。
- `GenPromptBar.tsx:128,162,184,190,211`：发送主 CTA、历史/上游面板按钮。
- `ChatNode.tsx:34-54,79-81`：联网/思考/清空；`.send-btn` 连 `title` 都没有。
- `ComfyConfigPanel.tsx:90,104`：模板管理齿轮、`.gd-send` 运行钮。
- 各节点头部：打光/多角度/角色卡重置钮、复制钮（`RelightNode:59,171`、`MultiAngleNode:53,154`、`CharCardNode:62,117,122,130`、`EcomImageNode:280,286`、`AudioNode:64,67`、`ComfyNode:50`、`GroupNode:52,57,66`、`GenConfigPanel:279`）。
- `canvas.css:2052-2061`：组节点头部按钮无 `:focus-visible` 可见态。

### A.6 交互硬伤（缺 `nodrag` / 键盘不可达）

- 缺 `nodrag`：`RelightNode.tsx:79-89`（chips）、`:104-133`（色块行）、`MultiAngleNode.tsx:75-84`、`VideoDubNode.tsx:53`（`.lang-seg`）、`PromptNode.tsx:63-72`（`.img-chip`）、`StylePresetNode.tsx:61`（已选列表，同时无法框选复制）。
- 裸 `div onClick`：`ImageNode.tsx:71-78`、`VideoNode.tsx:88-95`、`AudioNode.tsx:78`（`.img-empty` 导入区，系统性）；`StoryboardNode.tsx:53`（`.sb-cap` 折叠头）；`ChatNode.tsx:116-118`（无 href 的来源链接 `<a>`）。

### A.7 信息层级 / 大段文字

- `CharCardNode.tsx:99-102`：80+ 字说明塞 `.gen-sum` 单行省略，永远看不全 → 改可换行 hint。
- `EcomImageNode.tsx:307-314`：关键操作指引同样被 `.gen-sum` 截断且无 `title` 兜底。
- `DirectorNode.tsx:75-77`：空态 60+ 字产品说明 → 图标 + 短句。
- `StoryboardNode.tsx`：360px 宽堆 10+ 控件，「完善故事/风格定调」应收进底部参数栏。
- `RelightNode.tsx:157-165`、`MultiAngleNode.tsx:140-148`：重新生成时旧结果被骨架整体替换，应保留旧图。
- `VectorizeNode.tsx:34-37`：长任务仅一行文字进度，应对齐 ComfyNode 三件套。
- `StoryboardNode.tsx:113`：进度塞主按钮文案，应走独立 progress-line。
- `GenConfigPanel.tsx:907-911`：「绿口/橙口」文案与已统一的单色端口脱节，删颜色指引。

### A.8 死代码 / 内联 style 债

- 死内联 `acts style={{ opacity: 1 }}`（被 `.acts{display:contents}` 拍平）：`AudioNode:63`、`VideoDubNode:44`、`ChatNode:33`、`ComfyNode:49`、`CharCardNode:59`、`RelightNode:57`、`MultiAngleNode:51`、`EcomImageNode:277`。
- `GenConfigPanel.tsx` 12 处内联布局；`StoryboardNode.tsx:43-145`、`ComfyNode.tsx:139-184`（ParamField）、`CombineNode.tsx:25-47`、`NoteNode.tsx:27-36`、`VideoDubNode.tsx:61-64`、`StylePresetNode.tsx:38,61,65`、`MultiAngleNode.tsx:74` 内联 `maxHeight`。
- `GenPromptBar.tsx:94`：`style={{ flex: 1 }}` 与 CSS 重复。
- `EcomImageNode.tsx:303-305`：`.ecom-status` 自带 margin 又与父 gap 叠加，间距翻倍。
- `VectorizeNode.tsx:40`：节点主图唯一 `<img>` 直塞 dataURL（SVG 例外需注释说明）。
- `EditSurface.tsx`：MaskOverlay/CropOverlay 缺 `aria-label`（MarkOverlay 有）；蒙版红 `#ff3355` 等画布绘制色建议提常量注释。
- `canvas.css:876` 等：`--border` 相关回退值深浅色系混用（见 A.1）。

### A.9 正面样板（可推广，勿改坏）

- `ComfyNode.tsx:75-86`：progress-line + 百分比 + 进度条三件套（运行态标准）。
- `AudioGenNode.tsx:42-49`：头部动作「图标 + 文字」`.nt-btn`。
- `CharCardNode.tsx`：`cc-deliv` 素材交付卡（Thumb + 灯箱）。
- `ImageNode` / `VideoNode`：工具条「图标 + 文字」与 Thumb 使用。
- `canvas.css:300`：`.nt-btn.primary` 的 color-mix 光晕（其他处修改范本）。

