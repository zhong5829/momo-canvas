# MOMO 智能画布

一款以「智能画布」为核心的 AI 创作工作站。单一画布范式：把图片、提示词、对话、生图、视频、ComfyUI 工作流都变成画布上的节点，连线即工作流。

> 技术栈：Tauri 2 (Rust) · React 19 · TypeScript · React Flow (@xyflow) · Zustand
> 目标平台：Windows（主）/ macOS（后续分发）

## 启动

```bash
pnpm install
pnpm tauri dev      # 开发调试
pnpm tauri build    # 打包发行（需明确要求时再执行）
```

## 功能地图

| 模块 | 说明 |
| --- | --- |
| 智能画布 | 输入（图片/视频/音频/提示词/风格预设/备注）· 智能（拼接文本/分镜）· 生成（生成图像/生成视频/生成音频/ComfyUI）· 编辑（打光/多角度，图片直接编辑走节点工具条：裁剪/重绘/扩图/尺寸/增强）· 视频（取帧/取段/拼接·时间线/配音）· 角色（角色卡）。对话与文本处理已并入右侧创作助手与提示词工具弹窗 |
| 画布模板 | 组一键存为模板（配置+连线），Ctrl+K / 双击菜单反复实例化；内置 4 套示例工作流播种画布 |
| AI 布线助手 | 工具栏魔棒：一句话生成连好线的工作流，方案先预览、确认才落画布 |
| 模型配置 | **服务商卡片**：一张卡 = 一个中转站/官方（Base URL + Key 只填一次），下设对话/绘画/视频/音频/语音识别五类槽位，每类可加多个模型；顶部「默认模型」五类各选一个，节点/助手不单独指定时全用它 |
| 多协议 | 对话：OpenAI 兼容 / Anthropic Claude / Google Gemini；绘画：OpenAI images / Gemini 生图；视频：智谱 / 硅基流动 / OpenAI 任务式；音频：OpenAI `/audio/speech`；语音识别：OpenAI `/audio/transcriptions`（本地 FunASR 服务同样兼容） |
| 资产库 | 独立模块：画布生成内容自动落盘收录；同一次多图生成自动折叠为叠放组卡，点击后聚焦展开（电商切片与最终长图同组）；分类分区、文件夹、标签、条件筛选、批量删除/移动/打标签；缩略图卡片 + 原分辨率预览（图片/视频/音频/PDF）；图片卡片可直接拖回画布变成图片节点 |
| 创作助手 | 右侧边栏，取代原对话节点：**聊天**（多模态、可拖入图片/视频/文字、上下文自动压缩、一键在画布生图）+ **Agent**（自动搜资料 → 抉择确认 → 出图出片 → 发到画布）；顶部一行直选对话/绘画/视频模型 |
| 语音 | 语音输入（说一句转文字）与**语音通话模式**：VAD 自动断句 → 识别 → 助手回复 → TTS 朗读 → 自动续听，说话即可打断朗读 |
| 联网搜索 | Tavily / 博查 / SearXNG；模型自带联网（GLM / MiniMax / 混元等）优先走模型自己的搜索，失败自动降级 |
| 图片保存 | 保存目录、PNG/JPG/WebP、命名模板、生成后自动保存（独立于资产库收录） |
| ComfyUI | 直连本地服务；模板编辑器为**中文节点示意图**（连线关系可视、图/文入口与输出自动识别标记、可忽略节点/改输出/改默认值）；运行时上游图片自动喂给 LoadImage（缺图片入口时自动注入）、提交前按 object_info 校验并给中文报错；WebSocket 实时节点级进度条 |
| 生成记录 | 会话内时间线（右侧坞），完整历史在资产库 |
| 运行日志 | 标题栏活动图标：每次对外请求的方法/状态/耗时/请求体/响应体（密钥脱敏、轮询合并），排查中转站问题第一入口 |
| 批量出图 | 生成设置面板「批量出图」：一行一条提示词并行克隆生成；另有多模型对比 |
| 提示词历史 | 生成成功自动收录，提示词/生成节点上一键回填，支持搜索与收藏置顶 |
| 自动更新 | 设置→关于与更新：安装版走签名更新包自动安装，便携版下载 zip 自动替换重启；启动后台静默检查 |
| 主题 | 云白 / 深空蓝 / 深邃黑，标题栏或 Ctrl+Shift+T 一键切换 |

## 画布操作

| 操作 | 方式 |
| --- | --- |
| 平移 / 缩放 | 中键/右键拖拽 或 Space+左键；滚轮缩放 |
| 框选 | 左键拖拽空白处；Shift/Ctrl 加选 |
| 添加节点 | 双击空白 · 底部工具坞点击/拖放 · 从输出端口拖线到空白处 |
| **贴近自动连线** | 把节点拖到另一节点的左/右侧贴近（≤90px 且纵向有重叠）松手，自动按端口类型连线 |
| **拖线强吸附** | 从端口拖出连线，松手落在目标节点身上任意位置即自动接到匹配类型的端口（从输入口反向拖到上游节点同样生效），不必对准端口点 |
| 撤销 / 重做 | Ctrl+Z / Ctrl+Y（右下角也有按钮） |
| 搜索节点 | Ctrl+F：按名称/提示词/内容搜索，回车飞越并选中 |
| 快速添加 | Ctrl+K：Spotlight 键盘流搜索节点/画布模板并插入 |
| 导入图片/视频 | 拖入文件（图片、mp4/webm/mov 等视频）/ Ctrl+V 粘贴图片 / 节点上点击导入；视频自动落进资产库拿持久地址 |
| 复制 / 删除 | Ctrl+D / Del；适应全部：F；沉浸模式：Tab |
| **Alt 拖拽复制** | 按住 Alt 拖节点 = 复制一份跟手走，原节点不动；多选后 Alt 拖整组复制且保留内部连线 |
| **对齐吸附** | 拖动时自动吸附到其他节点的边/中线并显示参考线；A 键把所选节点强制对齐 |
| 前后对比 | 编辑类节点（重绘/扩图/抠图/增强/打光/多角度）点开大图后「对比原图」拖分割线擦看 |

## 数据流转规则

- 端口已统一（不再按类型拒连）：任意输出可接任意输入，实际传什么由源节点的输出类型决定；防环与重复边仍会拦截。
- 生成节点提示词留空时，自动取上游文本（提示词/分镜/拼接/风格预设等）。
- 拼接文本会递归物化其上游；风格预设输出选中片段的英文串。
- 生成节点连接上游图片时自动转为图生图（ComfyUI 依次填入图片参数并自动上传）。
- 每个生成/智能节点可在节点上单独选择模型卡片，默认跟随「设置 → 模型配置」中的单选默认。

## 目录结构（模块化）

```
src/
  core/
    services/      llm(3协议) / imageGen(2协议) / videoGen(3协议) / audioGen(TTS) / asr(语音识别)
                   webSearch / comfy / imageSaver / assetFiles / customProto / protoCalibrate / protoSelfHeal / http
    stores/        settings(服务商卡片 v4) / board(撤销重做+吸附+克隆) / comfy / assets / ui / agent / genPref / promptHist
    runner.ts      节点运行引擎（上游递归收集 → 调服务 → 回写 + 收录资产库）
    agentEngine.ts 创作助手：聊天（多轮+上下文压缩）与 Agent（JSON 动作协议循环）
    voiceChat.ts   语音通话状态机（录音 VAD → ASR → 助手 → TTS → 续听，可插话打断）
    modelMeta.ts   绘画家族参数推断 + 对话模型能力（视觉/自带联网）
    stylePresets.ts 内置风格片段库
  modules/
    canvas/        SmartCanvas + 节点集 + 添加坞 + 快速菜单 + 底部设置面板（图像/视频/音频/ComfyUI）+ 命令面板
    agent/         右侧创作助手面板（聊天 / Agent / 语音通话）
    assets/        资产库（分类/文件夹/筛选/批量/全格式预览）
    charlib/       角色库（内置人物预设 + 预览图缓存）
    shell/         标题栏 / 生成记录坞 / 报错中心 / 运行日志
    settings/      设置面板（默认模型总览 + 服务商卡片 + 协议 + 快捷键…）
    comfy/         工作流模板管理器
  ui/              SVG 图标集 + 轻量组件 + ModelPicker + PopSelect/PopLayer + Thumb/VideoThumb
  styles/          设计令牌（三主题）+ 基础样式
src-tauri/         Rust 壳（dialog / fs / http / store / opener + asset 协议）
```

## 数据存放

- 设置 / 画布 / 模板 / 资产索引 / 面板参数记忆（`gen-prefs.json`）/ 助手模型选择（`agent-prefs.json`）：AppData（tauri-plugin-store JSON）。
- 资产文件：`AppData/site.jinpengi.momo/assets/`（缩略图在 `assets/thumbs/`），删除资产会同步删除磁盘文件。
- 本地超清模型（ONNX）：**必需 8 个（~230MB）内嵌安装包**（`bundle.resources` 释放到 exe 同级 `models/`，便携 zip 同样附带），开箱即用；可选模型（GFPGAN/CodeFormer 各 ~350MB）按需下载到 `AppData/models/`。运行时解析顺序：AppData 副本 > 内嵌 > 自动下载（下载带 SHA-256 校验）。
- API Key 以明文存本机配置文件，请勿把 AppData 目录同步到公共位置。
- **密钥不进安装包**：所有配置只在本机 AppData，分发安装包/便携包给他人时对方是全新空白配置。

## 路线图

- [x] ComfyUI WebSocket 实时进度 / 节点级进度条
- [x] 资产库标签系统与拖拽入画布
- [x] 资产卡拖拽（原生拖出：画布 / 快捷栏 / 资源管理器 / 第三方软件一拖通吃）+ 右键快捷菜单
- [x] 打光节点（球面光源 / 六向按钮 / 亮度颜色 / 轮廓光 / 智能模式，出图↔提示词双输出）
- [x] 多角度节点（预设视角 + 环绕/俯仰/景别自定义机位，出图↔提示词双输出）
- [x] 角色卡节点（图片或文字描述 → 档案 + 三视图/表情/立绘/设定卡整套生成，设定卡风格可「自动」匹配）
- [x] 角色库（内置风格统一人物预设，预览图生成后落盘缓存，一键应用至画布）
- [x] 图片编辑节点组：局部重绘（蒙版编辑器/多候选选卡，GPT 真 mask + 指令式降级双通道）· 扩图 · 抠图（GPT 透明底）· 高清增强 · 聚焦裁剪
- [x] 画布搜索（Ctrl+F 飞越选中）+ Spotlight 快速添加（Ctrl+K，节点/模板一起搜）
- [x] 画布模板：组一键存模板、双击菜单/Spotlight 插入、内置示例工作流播种
- [x] 多模型对比出图（同输入并行多模型卡）· 创意度滑杆（图生图力度直觉化）· 资产库 Remix（按参数快照还原生成节点）
- [x] AI 布线助手：一句话生成工作流方案 → 预览确认 → 落画布（全部为普通节点可继续手调）
- [x] ComfyUI 模板编辑器 2.0：中文节点示意图 + 图/文入口自动识别 + 忽略节点/改输出/改默认值 + 运行时自动喂图与提交前校验
- [x] 运行日志面板（请求/响应流水，脱敏 + 轮询合并 + 落盘）
- [x] 批量出图（多行提示词并行）· 前后对比滑块 · 提示词历史/收藏
- [x] 模型家族扩充：seedream / flux / qwen-image / 可图 专属尺寸预设
- [x] 常用中转站协议预设（65535 / APIMart）一键导入修复
- [x] 自动更新（安装版 updater + 便携版 zip 替换）
- [x] 视频端口体系（第三种端口·绿色）：生成视频/取帧/取段/拼接/ComfyUI 全链路可连
- [x] 视频参数家族化：Sora/Veo/Luma/可灵/Seedance/Wan/Vidu/海螺/CogVideoX/PixVerse 按官方文档出时长/分辨率/比例/音频面板 + 首尾帧（第 1 路图=首帧、第 2 路=尾帧）
- [x] 视频取帧（末帧续接长视频）· 视频取段 · 视频拼接（本地重编码，实验性）· 视频批量出片
- [x] ComfyUI 视频通道：上游视频自动上传喂给 LoadVideo，VHS 合成视频回收展示（SeedVR2 视频放大打通）
- [x] 分镜短片流水线内置模板（风格定调 → 分镜出图 → 图生视频 → 末帧续接 → 拼接成片）
- [x] 视频协议能力体检 + 一键补全（首帧/尾帧/时长/分辨率占位符，与图片协议同待遇）
- [x] 分镜节点：故事→完善→按风格/定调拆分镜（带时间轴），逐镜独立输出口 + 一键铺生图/视频节点
- [x] 视频时长三态选择：家族预设档 + 范围滑块 + 自定义秒数输入（Seedance 2-15、Vidu 1-16、可灵 3-15…）
- [x] 视频源节点（本地视频拖入/导入即节点，asset 地址跨重启）+ 视频结果落盘持久化（生成/取段/拼接/Comfy 视频全部收进资产库并换本地地址，blob/直链过期不再丢片）
- [x] 视频缩略图体系：节点/画廊只渲染封面帧不挂 `<video>`，灯箱点开才播放；组框增加视频出口
- [x] 画布大图外置存储：巨型 dataURL 不再内联进画布 JSON（4K 图导致画布抖动/掉帧的根治）
- [x] 音频链路：音频源节点 + 生成音频（TTS `/audio/speech` + 自定义协议 `{{voice}}`，音频模型角色/协议/校准全套）+ 视频配音（本地混音，替换/混合原声）+ 生成视频参考音频（橙色口 · `{{refAudio}}`）
- [x] 时间线粗剪：拼接节点自带片段条（封面帧+时长、←/→ 调序、顺序连播预览成片）
- [x] 工作流文件分享：画布模板导出/导入 `.momoflow`（Ctrl+K 列表内导出，底部导入）
- [x] 提示词 @ 引用胶囊化（缩略图+图N 内联展示）· 视频节点提示词历史回填 · 多模型对比支持视频
- [x] 生成节点 LibLib 式改版：画布上只留结果卡，提示词/参考图胶囊/发送按钮并入底部生成面板（图像/视频/音频，音频新增独立面板）
- [x] 连线强吸附：拖线松手落在节点身上即自动接匹配端口（含输入口反向拖），端口吸附半径加大
- [x] 并行生成（同参数并发 N 条，部分失败保留成功结果）· 拖动对齐吸附（参考线 + `A` 键强制对齐）
- [x] 面板参数记忆（`gen-prefs.json`：比例/分辨率/数量/并行等，新建同类节点自动继承）
- [x] 侧边创作助手：取代对话节点，聊天/Agent 双模式 + 多模态 + 联网（模型自带优先）+ 上下文自动压缩 + 一键在画布生图
- [x] 语音链路：语音识别（asr）模型角色 + 语音输入 + 语音通话模式（VAD 自动断句 → 识别 → 助手回复 → TTS 朗读 → 自动续听，可插话打断）
- [x] Alt + 拖拽复制节点（多选整体复制，内部连线保留，原节点不动）
- [x] ComfyUI 节点 LibLib 式改版：模板/参数/上游计数并入底部设置面板
- [x] 默认模型总览（五类用途一处设定）+ 标题栏功能全量快捷键 + 快捷键设置分组两列
- [x] 超清放大节点：本地 DirectML 多模型融合超分（4K/8K/印刷尺寸 mm×DPI，非破坏、可取消、内容模式/细节强度可调）
- [x] 超清放大四档管线：极速 SPAN / 海报·文化墙 Nomos 保结构主链（细节融合手动开启）/ 人像 RealPLKSR+SCRFD+原貌回注（FaceUpDAT 显式开启）/ 印刷精修保守融合（DAT2 仅限手动插画细节）；含条件 DeJPG、固定形状 Tile、Session LRU、OOM 降级、8×以上自动禁用细节模型与融合伪影守卫（16 位为输出容器，模型源采样仍为 8 位）
- [x] 图片生产工具：节点内“标记”支持画笔/点位/矩形/圆角框/颜色/透明度并就地合成；“智能分层”支持视觉语义定位、本地边缘细化、遮挡背景补全、透明 PNG 提取、真实分层 PSD 与多页 TIFF 导出
- [x] 图片质量门禁：`pnpm quality:images` 对本机黄金样本计算 PSNR / 分块 SSIM / 边缘 IoU / ΔE / 体积 / 耗时，输出 HTML 差异热图并以退出码拦截退化；`manifest.json` 仅引用 AppData 用户资产并已加入 `.gitignore`，不进入仓库或发行包
- [x] 智能矢量节点：本地 VTracer 位图转 SVG（Logo/打卡框/文化墙/扁平插画，可导出 SVG/PDF/AI/CDR，点击才入资产库「矢量」分类）
- [x] 智能矢量四档质量：极速单候选 / 标准最多 3 候选 / 高保真最多 5 候选 / 少节点简化候选；1024px 重渲染评分、达标早停、预算内候选硬优先、黑白 Otsu 变体、可取消，并复用上游 analysisMap + vectorGuide
- [x] 矢量几何图元扩展：椭圆/圆角矩形拟合后必须通过最终重渲染质量守卫才接受；EPS 独立导出（渐变降级纯填充，不依赖 AI/CDR）
- [x] 图片编辑收编：裁剪/重绘/增强/扩图/尺寸收进悬浮「编辑」双列菜单；超清放大/智能矢量参数并入底部编辑面板
- [x] 超清模型内嵌安装包：公开发行白名单 4 模型（约 125MB，ESRGAN / RealPLKSR / SPAN / DeJPG）随 NSIS/便携 zip 分发，resourceDir 三级解析（AppData > 内嵌 > 下载）；UltraSharp、FaceUpDAT、SCRFD 与生成式人脸模型保持按需下载并显示许可状态

### 导演台与本地创作编排（计划）

产品边界：导演台负责剧情结构、提示词编排、生成调度、版本确认、缺片检查和剪辑交付；不复刻 Premiere/剪映，不内嵌 ComfyUI 节点编辑器，也不做 Blender 替代品。ComfyUI、Ollama、第三方视频模型和剪辑软件均以外部连接器接入。

#### 阶段 0：先稳定底座

- [x] ComfyUI 外部连接器收口：配置本地地址、健康检查、工作流导入、素材上传、任务提交、状态查询与结果回收；工作流修改继续在外部 ComfyUI 完成
- [x] ComfyUI 模板智能解析 v3：优先读取 APP Mode/子图声明，再结合 `object_info`、节点类型与拓扑自动识别图片、视频、音频、正/负提示词、首尾帧、多参考和主/辅助输出（analyzeCapsV3 + autoSlots 自动绑定到 variant.slots）
- [x] 模板绑定向导：显示语义槽、目标节点、判断依据和置信度；高置信度自动配置，中低置信度人工确认，低成本试跑后按工作流指纹保存（comfyBindings.ts）
- [x] ComfyUI 子工作流分支：一个模板拆成可命名、着色和独立运行的分支，节点内以“主模板 → 子菜单”选择；支持 SeedVR2 图片/视频放大等同文件多流程（连通分量自动检测 + extractVariantWorkflow + 二级菜单）
- [x] Ollama 本地服务商：本地模型发现、聊天/提示词调用、隐私路由、显存释放和与 ComfyUI 大任务的资源冲突提示（原生 /api/chat 协议 + /api/tags 发现 + keep_alive 释放）
- [x] 本地 GGUF 直跑 llama-server：选文件自动配 mmproj 一键「添加并测试」，Rust 受控子进程（无 shell 拼接、仅 127.0.0.1、健康检查、退出清理），模型直进对话选择器可发图；运行中模型可看日志/停止释放显存（local_llm.rs + localGgufStore + 虚拟服务商注入）
- [x] 便携版首次启动自动创建桌面快捷方式（COM IShellLink 直写 .lnk，已存在即跳过）
- [x] MOMO Skill 基础设施：安全导入 `SKILL.md`/`.momoskill`、管理器、变量、适用范围、执行预览、结果快照与冲突检查，不执行上传脚本

#### 阶段 1：提示词与范例系统

- [x] 优秀范例拆解：导入提示词、场景案例、参考图或分镜案例，保留原文并拆成主体、场景、动作、景别、构图、镜头、光线、色彩、风格、连续性和负向规则（directorAnalysis.ts breakdownPrompt + 脚本页拆解区）
- [x] 提示词配方库：范例拆解结果可修正、命名、分类、评分和复用，并记录适用模型与参考效果；界面不使用含义不清的“Scale”术语（PromptRecipe + applyRecipeToPrompt）
- [x] 模型无关提示词大纲：按“项目规则 + 场景规则 + 角色设定 + 镜头大纲 + 上一镜结束状态 + 配方 + 负向规则”生成可追溯结构（directorPrompt.ts compileShotStructure）
- [x] 提示词编译器：将同一结构分别编译为 MiniMax H3、Seedance/中转站、ComfyUI 图片/视频工作流及普通海报等目标格式，支持模块开关、去重和冲突提示（compilePrompt + injectR2VTags）

#### 阶段 2：导演台生成闭环

- [x] 导演台项目节点与独立持久化：画布节点只显示项目摘要和入口，项目数据按“剧集/场景/剧情段/镜头/Take”独立保存
- [x] 外部剧本导入与确定性切分：支持 `分段1/分段2`、标题、空行或自定义分隔符，保留原文映射并检查超时、漏段和重复段（deterministicSplit）
- [x] 剧情覆盖与连续性状态：记录每镜进入/结束时人物站位、服装、道具、动作、情绪、光线和机位，提示缺失过渡、剧情跳跃及状态冲突（directorAnalysis.ts checkContinuity）
- [x] 模型无关生成配方：同一镜头可选择本地 ComfyUI、MiniMax H3、Seedance 2.0 中转站或其他第三方视频模型，切换时显式报告能力不兼容项（DirectorRecipe 类型 + capabilitySnapshot）
- [x] 批量任务队列：生成所选、生成缺失、生成已修改、失败重试；本地与远程分别限流，跨重启恢复并在提交前汇总可能计费（directorQueue.ts）
- [x] Take 版本确认：每次生成保留不可变快照，支持采用、锁定、备注、基于某版再生成；只有一个采用版本进入全片顺序（createTake/approveTake）
- [x] 故事串片预演：按镜头顺序硬切连播，只用于检查缺片、时长、剧情和连续性，不发展为多轨剪辑器（SeqPlayer 接通资产地址解析）
- [x] 批量质量检查与后处理：把已确认但分辨率、帧率或稳定性不足的镜头送入 ComfyUI 放大、补帧、修复分支并保留派生链（directorAnalysis.ts checkQuality + directorQueue.ts executePostProcess/runBatchPostProcess + 派生 Take derivedFrom 链）

#### 阶段 3：音频与剪辑交付

- [x] 音频导演台：对白、旁白绑定镜头，环境音/音乐绑定场景；支持角色声线、读音词典、情绪语速、多版本、字幕对齐和可选口型任务（DirectorAudioTrack + 剪辑页音频区 + TTS 生成）
- [x] 对白优先/画面优先两种时长策略：对白先定镜头时长，或在画面锁定后让 TTS 适配时长；默认鼓励视频无字幕、无内嵌背景音乐（directorExport.ts dialogueFirstStrategy/videoFirstStrategy）
- [x] 标准项目交付包：按稳定镜头 ID 输出已确认视频、对白、旁白、音效、环境音、音乐、SRT、镜头清单、预演片和素材校验清单（剪辑页交付包结构 + 资产收集）
- [x] Premiere XML 交付：生成可导入的 Final Cut Pro 7 XML，预排 V1 视频与对白/旁白/音效/环境音/音乐轨道，并写入场景、镜头和剧情段标记（directorExport.ts generatePremiereXml）
- [x] Premiere MOMO Bridge（后续可选）：通过 UXP 插件一键导入素材、创建素材箱/序列/轨道/标记；不把 `.prproj` 私有格式作为首版目标（uxp-bridge/ 独立插件骨架：manifest + 面板 + 读清单建箱/序列/标记）
- [x] 剪映稳定交付包：编号素材 + SRT + 音频分轨 + 镜头表 + 预演片；私有草稿写入仅作为按版本适配、先备份、可回退的实验功能（SRT/CSV 镜头表/项目清单 JSON 已实现，私有草稿格式不触碰）

#### 阶段 4：轻量 3D 站位参考

- [x] 3D 站位参考器：导入现成 GLB/GLTF/VRM 或低模替身，调整站位、朝向、预设姿势、简单道具、相机、焦段和光源方向（SVG 俯视图实现，Three.js GLB 渲染为后续增强）
- [x] 3D 控制图导出：输出彩色站位图、轮廓、深度、法线、角色分区和姿势图，并映射到 ComfyUI 的 `layoutGuide`/`poseGuide` 等语义槽（彩色站位图 + 灰度深度参考图 + 语义分区图已实现，基于俯视图零依赖方案）
- [x] 明确不做建模、骨骼绑定、复杂动画、材质节点、雕刻和专业渲染；高级制作继续交给 Blender

#### 阶段 5：导演台深度迭代（v0.6.0）

- [x] ComfyUI 往返编辑：零配置推送（写 ComfyUI 工作流库 + 自动打开）→ Ctrl+S 自动同步回模板，节点位置双向保留
- [x] 剧本三态导入：完整剧本（LLM 拆分）/ 已分段分镜脚本 / 成品分段提示词包直录（显式围栏 + 启发式识别，风格/定调小节自动提取）
- [x] 批量生成实时进度（总进度 + 当前片段双条，ComfyUI WebSocket 按节点/步数换算）· 分段/最终提示词弹窗编辑 · 成片检查四步流程与高清放大参数
- [x] 导演台空间接力：相邻上一段采用/最新成功版本 → 稳定桥接帧 + Ref2VA末尾2秒参考视频 → 下一段参考槽与运行时 `<Picture N>/<Video N>` 空间锁定；所选/缺失批量不会跨非相邻片段错接
- [x] 导演台双语资产册：`资产提示词.md` 一次导入真实图片+中英文提示词+使用分段；唯一素材去重、站位图空间角色、图片槽满时自动转文字空间锁并让真实桥接帧优先
- [x] 参考槽悬停预览（图/视/音浮层）· 版本卡采用开关 · 死配方自动清理 · 连续性检查豁免直录对 · 3D 页主题适配 · 设置页协议区卡片化与占位符一键复制

- [ ] 口型同步（等可灵/即梦对口型 API 按家族接入）
- [ ] SAM2/SAM2.1 内容分区、StarVector Logo 候选、diffvg 可微精修（均需 Python/PyTorch 线路，暂缓；其余文档模型已全部 ONNX 化落地）
- [ ] macOS 打包分发
