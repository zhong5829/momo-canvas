# MOMO 超清增强 · 使用说明

> 本地 GPU 超分放大，非破坏编辑。当前为 **阶段一（单模型 MVP）**，已通过 `cargo test` 在 RTX 5090 + DirectML 上连跑两遍验证。

## 一、它能做什么

把画布上的图片用**本地 GPU**（不走云端、不上传）放大到 4K / 8K / 16K，适合海报、文化墙、照片、插画等大尺寸输出。

- **完全本地**：图片不出本机，不依赖任何云 API、不消耗中转站额度。
- **非破坏**：原图永远不被修改，放大结果是画布上的一个**新资产**。
- **DirectML 加速**：走 DirectX 12，N 卡 / A 卡 / I 卡通用；在 RTX 5090（Blackwell, sm_120）上这是当前唯一能跑的 GPU 路径（CUDA EP 暂不支持 sm_120）。
- **自动 Tile**：大图切块推理 + 三角窗无缝拼接，不爆显存。
- **可取消、有进度**：节点上显示阶段 + 百分比，随时可停。

## 二、两种入口（共用同一引擎）

### 入口 1：节点
底部工具坞 / 快速添加菜单 / Spotlight 里找 **「超清增强」**（编辑分组，🔍 图标），或右键空白处添加。把它放到画布上。

### 入口 2：右键快捷
**右键任意图片节点 / 生成图像节点** → 「编辑处理」分组 → **「超清放大」**。会自动在右侧新建一个超清增强节点并连好线。

## 三、怎么用（三步）

1. **连线**：把一张图片节点（图片 / 生成图像 / 多角度等任何出图的节点）的输出端口，连到「超清增强」节点的左侧输入端口。
2. **选参数**（节点上）：
   - **目标**：4K（3840 长边，默认）/ 8K（7680）/ 16K（15360，实验）。
   - **质量**：极速（单模型）/ 标准 / 专业（主+细节融合，区别在细节权重）。
   - **格式**：PNG（默认）/ **TIFF（印刷）** / JPEG。
   - **目标**：4K / 8K / 16K / **印刷尺寸…**（输 宽mm×高mm×DPI，自动算像素）。
   - 完成后报告里写 `类型:photo/illustration/poster 边缘:X% 压缩:X%`（自动内容分析）。
3. **点「增强」**：节点进入运行态，显示「加载模型 → 解码输入 → 合并 Tile → 精确缩放 → 编码输出」阶段 + 进度条。完成后节点上直接显示放大后的图，下方一行报告：`N Tile · directml · X.Xs · W×H`。

放大、保存：节点右上角「放大」开灯箱看原图、「保存」存本地。结果同时收录进左侧资产库。

## 四、模型管理（自动）

- 首次点「增强」时，若 `%AppData%\site.jinpengi.momo\models\` 下没有模型，会自动从国内镜像（hf-mirror.com）下载并校验 **大小 + SHA-256**，通过后才用。
- 内置模型（均已预拷到 AppData，首跑免下载）：
  - `NomosWebPhoto · 4x`（主放大，64MB）—— 标准/专业预设用。
  - `UltraSharp V2 Lite · 4x`（细节/极速，28.5MB）。
- 模型文件不入 git（`models/` 已在 `.gitignore`）。
- **设置 → 超清模型** 可查看每个模型的在位状态、重新下载、删除。

## 五、排错

| 现象 | 原因 / 处理 |
|---|---|
| 「本地超清增强仅桌面版支持」 | 浏览器预览模式无本地推理；用安装版 / `pnpm tauri dev` 桌面端。 |
| 下载模型失败 / 校验失败 | 网络问题；可手动把 `models/sr/4x-UltraSharpV2_Lite_fp32_op17.onnx` 拷到 `%AppData%\site.jinpengi.momo\models\`。 |
| 显存不足 | 阶段一固定 Tile=512，5090 32GB 远够；低显存机型会自动回落 CPU（慢但能跑）。 |
| 「请把一张图片连到…」 | 节点没有上游图片输入；先连一张图。 |
| 16K 失败 / 极慢 | 16K 受浏览器单图 16384px 硬限 + 内存约束，标为「实验」；建议先用 4K/8K。 |

## 六、当前范围（阶段一 + 二 + 三已完成）

✅ 已完成并 `cargo test` 在 RTX 5090 + DirectML 上验证（10 个测试全绿）：
- **阶段一**：单模型 + DirectML + 笨 Tile；非破坏输出；节点 + 右键 + 编辑按钮三入口；进度 + 取消；Alpha 单独缩放回贴；模型注册表 + SHA-256 + 自动下载；设置迁移；`selfSig` 修缓存键隐患。
- **阶段二**：NomosWebPhoto 主 + UltraSharp 细节，内容掩膜路由 + 双频段融合；质量分析（JPEG/噪声/模糊/边缘/内容类型）；预设路由（极速/标准/专业 × 内容类型）。
- **阶段三**：
  - **输出格式 PNG/TIFF/JPEG**（TIFF 适合印刷工作流，JPEG 自动压平 Alpha）。
  - **印刷尺寸模式**：目标选「印刷尺寸…」→ 输入 宽mm × 高mm × DPI，自动算像素并按此输出。
  - **§5 倍率策略 + 内存上限**：大图不再卡死（目标≤原图自动跳过超分；否则预算模型输入使中间图 ≤6K）。
  - **模型管理 UI**：设置 → 超清模型，查看在位状态 / 重新下载 / 删除。
  - **内容分析 flatRatio（色块占比）**：报告里 `色块:X%`，海报/文化墙高、照片低。
  - **vectorGuide + analysisMap 资产契约**：标准/专业档额外输出"保结构引导图 + 内容分析 JSON"（落 AppData/assets，供智能矢量化节点复用）。报告末尾会写 `已输出矢量引导/分析资产`。
  - **ROI 细节推理**：细节模型自动跳过平坦色块 tile，不往色块里灌假纹理（文档 §21.4）。
  - **下拉统一 PopSelect**：目标/质量/格式 都是图标+文字+描述的弹层，与全应用风格一致。

⏳ 暂未做（按需，已说明）：
- **条件去 JPEG**：需 1x DeJPG ONNX 模型，有就能接（NomosWebPhoto 已能处理一般 JPEG）。
- **二维码检测+重建**：纯 Rust 可做（rqrr+qrcode），你的场景非刚需，按需加。
- **OCR 中文重排 / 文字重建**：重依赖（PaddleOCR/Tesseract）+ 中文识别需视觉验收，是独立子项目，暂缓。
- **TensorRT**：5090 sm_120 高风险，文档明示"不阻塞首版"，跳过。

## 七、验收清单（阶段一）

- [x] `cargo test sr::` 连跑两遍通过（384×256，4 Tile，DirectML）。
- [x] `npx tsc --noEmit` 通过。
- [x] Rust 编译通过（ort 2.0-rc.13 + DirectML）。
- [ ] 从节点启动增强，输出正确尺寸的图（请在应用里点一下）。
- [ ] 从右键「超清放大」启动，走同一引擎。
- [ ] 原图未被修改。
- [ ] 任务可取消，失败不崩画布。
- [ ] 改目标尺寸后重新运行，给的是新尺寸（`selfSig` 生效）。

## 八、技术一句话

Rust 侧 `src-tauri/src/sr.rs`：`ort` crate（ONNX Runtime）+ DirectML EP，笨 Tile 三角窗加权累积，输出原子写。前端 `runEnhanceLocal`（`src/core/runner.ts`）经 `@tauri-apps/api/core` 的 `invoke` + `Channel` 调命令、收进度；模型注册表在 `src/core/localModelRegistry.ts`。
