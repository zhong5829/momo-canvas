# MOMO 智能画布 · 优化文档

> 2026-08-13 生成（同日更新：3D 导演台定位、ComfyUI 多输入映射）。覆盖：导演台优化、资产库优化、ComfyUI 模板可视化、右键菜单审查、全软件审查。
> 结论先行，每节最后给「建议优先级」。文档尾部附**本轮已完成的修复清单**。

---

## 一、导演台优化（定位：批量出片，不是单条精剪）

导演台的设计定位已明确：**有剧本 + 资产，按剧本批量生成片段**；单条精出回画布做。以下建议全部围绕「批量」这条主线。

### 0. 3D 导演台定位：影片站位参考（新增 · 2026-08-13）

**3D 导演台不是「3D 建模/渲染工具」，而是「影片站位参考生成器」**：出一张影片（或一条短片）需要固定人物站位时——把人物模型在 3D 场景里摆好机位与站位 → 渲染一张**站位图** → 导出给生成模型当参考图。它与画布/资产库的闭环是：

```
3D 导演台：摆人物模型 + 定机位 → 渲染站位图 → 收入资产库
        ↓
资产库（导演台内可直接打开）→ 拖到画布生成节点 / ComfyUI 节点当参考图
        ↓
生成模型按站位图出片，人物位置一致
```

据此，3D 导演台的优化方向不再是「更强的 3D 编辑」，而是：

| 优先级 | 建议 | 说明 |
|--------|------|------|
| ★★★ | **人物模型库** | 内置/导入常用站位人物（站姿/坐姿/剪影人偶，无需精细建模），拖入场景即用；支持本地导入 glTF/OBJ |
| ★★★ | **站位图一键导出** | 渲染后一键进资产库并自动命名「站位参考 · 场景名」，直接作为画布生成节点的参考图（现已有资产库入口，补上导出快捷键/按钮即闭环） |
| ★★☆ | **机位预设** | 常用机位（全景/中景/特写/正打/反打）一键摆位，不用手动转视角 |
| ★★☆ | **站位标注** | 站位图上叠加「人物 A/人物 B 站位点」标注图层，导出时可带/不带标注（生成模型不需要标注，人看走位需要） |
| ★☆☆ | 多人物站位管理 | 多角色场景里每个角色独立站位、各自导出一张单人站位图 |

> 注意：上一版文档 §1.2 中「3D 导演台的参考图产物会进资产库」的闭环判断不变，本节是把它升格为**核心定位**。

### 1.1 现状问题

| # | 问题 | 影响 |
|---|------|------|
| 1 | 5 个页签是纯线性流水（剧本→分镜→生成→成片检查→3D），但没有「项目看板」概念：生成到一半要手动翻页看进度 | 批量出片时频繁切页，无法一眼看到整体状态 |
| 2 | 参考图槽（RefSlotsCard）接入点只有首帧/尾帧/参考图/姿势/布局五种 ComfyUI 语义，对「批量」场景偏少（缺景别、角色 ID、时间/光线标签） | 资产多时难以按剧本语义组织 |
| 3 | 生成队列（directorQueue）是隐藏的运行细节，没有可见的队列面板（哪些片段在跑、哪些失败待重试） | 批量跑到一半失败，用户不知道剩多少 |
| 4 | 3D 导演台的参考图产物会进资产库，但导演台内部没有资产库入口（本次已加底部按钮 ✓） | 已解决一半：入口有了，拖出/右键使用依赖资产库自身能力（见第二节） |
| 5 | 片段审核（采用/弃用）在生成页逐个点，没有「缩略图看板」批量勾选模式 | 批量出片时审核效率低 |

### 1.2 建议（按价值排序）

1. **生成队列可见化（高价值）**：导演台顶部进度条旁加「队列」面板——列出全部待生成片段（状态：排队/生成中/完成/失败），失败项一键重试、全部重试。批量场景的核心痛点。
2. **审核看板模式（高价值）**：成片检查页增加「看板」视图（缩略图网格，每格勾选采用/弃用，支持框选批量操作），替代现在的逐条点选。
3. **参考图语义扩展（中）**：REF_SEMANTICS 增加「景别（远景/中景/特写）」「光线（白天/夜晚/室内）」「角色（角色 A/B）」标签，接入剧本字段，生成时自动按镜头语义选图。这正好承接你说的「3D 导演台参考图 → 资产库 → 导演台调用」链路。
4. **剧本页支持拖入资产卡（中）**：剧本描述/镜头行支持从资产库拖入图片作为该镜头的参考绑定，自动进 globalSlots 并带镜头语义。
5. **导出报告（低）**：批量完成后导出「剧本 + 片段缩略图 + 采用状态」的 HTML/PDF 报告，方便交付沟通。

---

## 二、资产库优化

### 2.1 现状盘点（已有功能）

分类页签（含计数）、文件夹（新建/重命名/删除）、标签（筛选/批量打标）、全文搜索、生成分组卡、批量操作（移动/打标/删除）、预览层（重命名/标签/元信息/另存为/打开位置/删除）、右键菜单（放画布/Remix/发快捷方式/另存为/删除）、拖出到画布（仅图片/视频/音频）。

### 2.2 缺失与建议

| 优先级 | 建议 | 说明 |
|--------|------|------|
| ★★★ | **回收站** | 现在删除直接从磁盘删文件（assetStore.remove），误删无法恢复。建议：删除进回收站（AppData 子目录），30 天后真正清理；回收站页可恢复/彻底删除。改动量中等（assetStore 加 trash 列表 + 回收站页签），但价值最高——资产是创作原料 |
| ★★★ | **批量导出/打包** | 只有单资产另存为。建议：批量选择后「导出到文件夹」+「打包为 zip」（含元信息 json）。批量场景刚需 |
| ★★☆ | **网格内重命名** | 现在只能进预览层改。建议：卡片双击或右键「重命名」就地编辑 |
| ★★☆ | **组卡右键菜单** | 组聚焦面板成员的右键是完整 CardMenu，但**组卡本身没有右键**。建议：组卡右键 = 组内全部放画布 / 组另存为 / 删除组（含成员） |
| ★★☆ | **收藏/置顶** | 高频复用的参考图（如你常用来做角色一致性的脸/腿/胸局部图）置顶或收藏过滤 |
| ★☆☆ | **重复资产检测** | 入库时按内容哈希检测重复（画布反复生成会产生大量近似图） |
| ★☆☆ | **资产包导出** | 把一组资产 + 元信息打包成 .momoasset，可分享/迁移 |

### 2.3 与导演台的联动（承接第一节）

导演台底部「资产库」按钮已加（本次完成）。进一步建议：资产卡拖入导演台的「剧本镜头行 / 参考图槽」时直接绑定该镜头语义（而不是进画布）——即导演台内拖放目标有自己的处理逻辑，与画布拖放分开。

---

## 三、ComfyUI 模板可视化编辑

### 3.1 现状

编辑方式 = **静态 SVG 示意图（自动分层布局，只读）+ 右侧参数表单**。节点不可拖、不可连线、不可增删。模板本体是 ComfyUI 原生 API 格式 JSON，中文名靠词典翻译（CLASS_ZH 约 60 词），词典没有的词直接露英文 class_type。

### 3.2 「再简单一点」的建议

当前对非 ComfyUI 用户的最大门槛是**看到的是英文工作流结构**。在不做完整画布编辑器（成本高）的前提下：

1. **节点名全部中文化（高价值、低成本）**：扩充 CLASS_ZH / INPUT_ZH 词典（目前约 60 词，常见工作流 200+ 词条），并加**智能兜底**——词典没命中的 class_type 在示意图上显示「英文名 + 小字提示该节点的中文含义」，或直接让对话模型批量翻译一次并缓存进模板（模板里存 `_zhCache`，不依赖联网）。
2. **示意图加筛选/折叠（中）**：按「输入 / 采样 / 输出 / 控制网」分组着色；暴露参数的节点高亮，未暴露的节点折叠成一行「+N 个内部节点」。让「看」变简单。
3. **暴露参数编辑再简化（中）**：把右侧详情页的「类型/标签/默认值」三件套在**示意图节点上直接弹小表单**编辑（点节点就地改），少一层页面切换。

### 3.3 子工作流分支问题（你反馈的痛点）

现状：`autoVariantsFromWorkflow` 按弱连通分量自动拆分支（拆「断开的处理链」），每分支独立 nodeIds / 输出节点 / 参数；运行时按 variantId 只提交对应分支。

你反馈的痛点拆解：

| 痛点 | 根因 | 建议 |
|------|------|------|
| **分支多英文、不直观** | 分支名默认是「分支 1/2/3」+ 节点英文 class_type 列表；分支识别靠程序拆连通分量，用户看不到「为什么这么分」 | ① 分支命名智能化：按该分支的输出节点类型自动命名（「出图分支」「放大分支」）或按输入差异命名（「文生图分支 / 图生图分支」）；② 拆分时给每个分支生成一句话说明（它吃什么、出什么） |
| **「操作形式不对，容易混淆」** | 手动增删分支要懂 nodeIds 概念；分支与主链的关系不可视化 | 在示意图上用**不同颜色的虚线框**框出每个分支（分支 = 节点集合 + 语义槽），分支面板改成「点框选中 → 改名字/输出」的直观操作，不再裸列 nodeIds |
| **不够智能化** | 拆分支全靠图论，不理解工作流语义 | 加**智能拆分助手**（复用协议助手的模式）：让对话模型读工作流 JSON 输出「每套子工作流的名称、输入点、输出点、用途说明」，程序按模型结果落地分支结构。比纯连通分量拆得准，且命名天然是中文 |

优先级：① 分支中文命名与说明（高）→ ② 示意图分支框选（中）→ ③ 智能拆分助手（中，模型能力已验证过协议助手的可行性）。

### 3.4 多输入精确映射（新增 · 2026-08-13 · 高优先级）

**痛点**：工作流有多个传入点（例：一套流程 = 深度图入口 + 图生图入口，或首帧 + 参考图 + 控制网图）时，从画布拉入多张图，程序现在**按「暴露参数顺序 + 节点编号顺序」自动 shift 分配**（`src/core/services/comfy.ts:646-681` 的 imgQueue 逻辑），用户无法指定「这张图必须进深度图口、那张图进图生图口」，容易混淆出错。

**方案 A（推荐）：语义槽 + 可视化映射浮窗**

1. **数据模型**：`ComfyData` 加 `imageSlotMap?: Record<string, string>`（槽 key → 上游图 dataURL）。槽 key 复用模板已有的语义槽 `autoSlots`（`comfy.ts:149-184` 已自动识别首帧/尾帧/参考图 N/参考视频/参考音频/提示词，每个槽带 nodeId + input）。
2. **自动预分配（零回归）**：运行时先按现状顺序生成默认映射（图1→首帧、图2→尾帧、其余→参考图），写进 imageSlotMap 展示。
3. **映射浮窗 UI**（Comfy 底部参数栏加一个「输入映射」弹卡）：列出全部图片槽，每行 = 槽名（中文，如「首帧 · LoadImage #3」）+ 当前分配图缩略图；点缩略图弹出上游所有图的缩略图选择器，可换图/清空；支持拖拽换序。改完即时写回 imageSlotMap。
4. **执行**：`runComfyTemplate` 改为优先按 `imageSlotMap` 精确写入对应槽的 nodeId.input，只有未映射的槽才走旧的顺序 shift 兜底（老数据零回归）。
5. **节点上可见**：ComfyNode 显示「首帧 ✓ / 参考图 ✓」小徽章，一眼看出哪张图接进了哪个口。

**方案 B（轻量兜底）**：不做浮窗，把每个图片槽做成一个下拉选择器（「深度图 ← 上游图 1/2/3/无」），直接嵌在 Comfy 参数面板的图片参数区。实现成本低，但多槽时不如 A 直观。

**实施路径**（方案 A）：① types.ts 加 `imageSlotMap` → ② ComfyConfigPanel 加「输入映射」弹卡（缩略图选择器复用角色卡「生成参考图」的 AssetPickGrid 同款模式，改造成「从上游图列表选」）→ ③ comfy.ts 的图片分配改为 slotMap 优先 → ④ ComfyNode 加映射徽章。

**配套建议**：模板编辑器里给每个图片入口节点**改中文标签**（如把 LoadImage #3 标成「深度图」），槽名直接用人看的名字——与 §3.1 的词典中文化同一批做。

---

## 四、右键菜单审查

### 4.1 现状（画布）

通用 ContextMenu 组件（自动贴边、Esc/外部点击关闭）已完备。节点菜单全部类型共用基础 4 项（复制/删除/忽略/适应）+ 按 RUNNABLE / MEDIA / image 集合追加。

### 4.2 缺失项清单（本次审查发现）

| # | 缺失 | 建议 |
|---|------|------|
| 1 | **ecomImage（电商长图）、director（导演台）不在 RUNNABLE 集合，右键不能「运行此节点」** | 加入可运行集合（runner 已支持 runEcom / runDirector？导演台节点双击打开工作台而非运行——确认语义后：电商长图应可运行；导演台右键加「打开导演台」） |
| 2 | 导演台节点右键没有「打开导演台」 | 加：右键 → 打开导演台（等价双击行为） |
| 3 | prompt / note / chat / stylePreset / group / ecomImage / director 等 7 种类型菜单完全无差异化项 | 建议：prompt/note 加「复制文本」；group 加「展开/收起」「解散组」；chat 加「清空对话」 |
| 4 | 图片节点右键有「保存到资产库」，但生成节点没有「重新生成」（需要进节点点按钮） | 生成类节点加「重新生成」快捷项 |
| 5 | 组节点菜单无「组内全部运行」「组内全部停止」 | 批量场景常用 |
| 6 | 连线（边）没有右键菜单 | 加：删除连线 / 反转方向？至少「删除连线」值得做 |
| 7 | 资产库**组卡**无右键菜单（见 §2.2） | 已列 |
| 8 | 生成图像节点右键的「编辑处理」组只有 4 项（打光/多角度/超清/矢量），缺「电商长图」——从商品图一步生成详情页是自然操作 | 图片节点加「生成电商长图」 |

### 4.3 已完善的（本轮）

创作助手消息气泡右键菜单（框选文字 → 建成提示词/备注节点、复制选中文字、整条在画布生图）。

---

## 五、全软件审查

### 5.1 Bug（本轮已修复）

| Bug | 根因 | 修复 |
|-----|------|------|
| 打开角色卡节点整窗白屏 | CharConfigPanel 的 useBoard 写在条件 return 之后，hooks 数量随选中态变化 | hooks 上移到 return 之前；底部参数栏整体包 ErrorBoundary 兜底（以后任何面板异常只坏面板不白屏） |
| 本地 GGUF 模型在界面上选不到 | ModelPicker 直接读 settings.providers，没有注入 local-gguf 虚拟服务商 | ModelPicker 改走 providersOfRole（含注入），节点/面板/默认模型全可选 |
| Ollama 预设 logo 显示乱码 | `logo.slice(0,1)` 把 🦙 emoji 切成半个代理对 | 按码点取首字符 `[...logo][0]`（预设卡与服务商卡两处） |
| 模型配置出现两张相同 Ollama 卡 | 固定入口卡 + 用户配置的服务商卡并存 | 已配置 Ollama 卡时固定卡让位 |
| 全屏下画布历史弹窗超出窗口被切割 | 弹窗锚定标题栏标签条（贴左上）向左展开，宽度大时溢出左边界 | 改为相对视口水平居中（fixed + translateX），永不越界 |
| 创作助手消息无法框选文字 | 全局 `user-select: none` 未在消息气泡放开 | `.ag-bubble` 加 user-select: text + 右键建节点菜单 |

### 5.2 逻辑 / 操作流畅度问题（未修，按优先级排）

| # | 问题 | 建议 | 影响评估 |
|---|------|------|----------|
| 1 | 无「打开导演台」快捷键（已修 ✓），但导演台内 5 个页签无快捷键 | 页签 Ctrl+1..5 | 小，顺手做 |
| 2 | 底部工具坞 16 个节点，快捷键在 title 里有但工具坞按钮上不显示 | 工具坞按钮加小键帽角标（同 AddNodeMenu 本次加的做法） | 小 |
| 3 | 生成节点运行时「停止」按钮在节点上很小，批量跑错想全停要进右键菜单 | 底部生成栏运行时显示「停止全部」按钮 | 中 |
| 4 | 撤销/重做无历史长度提示（撤销到哪了不知道） | 标题栏按钮 title 显示上一步操作名 | 小 |
| 5 | 画布节点多时拖拽仍偶有掉帧（大图直塞 img 的历史遗留点） | 全仓再扫一遍未用 Thumb 的节点内图片 | 中，需逐节点查 |
| 6 | 报错中心「全部清除」能力在 store 里有（clearErrlog），需确认 UI 是否有入口 | 若 UI 无按钮则补一个 | 小 |
| 7 | ComfyUI 节点暴露参数多时，节点上表单很长 | 参数分组折叠（输入/采样/输出三组） | 中 |

### 5.3 新功能建议（中远期）

| 建议 | 价值 |
|------|------|
| 画布「收藏模板」：把任意一组节点存成模板（现在只有 ComfyUI 模板和画布模板概念） | 复用工作流 |
| 节点「运行计时 + 费用」徽标（每个生成结果带耗时/预估费用） | 透明化成本 |
| 图片节点「对比视图」：同图多版本并排切换 | 挑图效率 |
| 语音转录节点（视频/音频 → 字幕文本） | 补齐媒体链路 |
| 提示词历史库（跨节点检索用过的提示词） | 复用语料 |

---

## 五·补、二轮审查补充清单（2026-08-13 追加）

### 5.4 工程与健壮性（防「改代码就出事故」的根）

| # | 问题 | 建议 | 影响 |
|---|------|------|------|
| 1 | **无 ESLint（react-hooks 规则）**：本轮白屏事故的根因（hooks 写错位置）编译期完全可拦截 | 引入 eslint + `react-hooks/rules-of-hooks`，最少成本：只跑 `npx eslint` 不配 IDE | 高——这是「经常改出白屏」的制度性解法 |
| 2 | **无任何测试**（项目现状，CLAUDE.md 已注明） | 至少给核心纯函数（modelMeta / wfGraph / protoSpec 校验 / persist 迁移）配 vitest 单测；UI 侧靠 ErrorBoundary + 手测 | 中，渐进投入 |
| 3 | **RUNNERS（引擎）与 SmartCanvas RUNNABLE（UI）是两张表**，本轮刚修了它们漂移出的 bug（ecomImage 漏、combine 多） | 把 RUNNABLE 导出为 runner.ts 的单一来源（`export const RUNNABLE_KINDS`），UI 引用它 | 高，一行代码的架构债，已复发一次 |
| 4 | **快捷键冲突无检测**：设置里两个动作绑同一个键静默生效 | HotkeysTab 保存时检测冲突并提示（标红冲突项） | 中 |
| 5 | **撤销快照 = 60 份全量 nodes（含 dataURL）**（boardStore:629） | 快照瘦身：图片字段外部化引用，或按总字节数动态降低条数 | 中——大画布 + 多图时内存/落盘双压力 |
| 6 | API Key 明文存 AppData（已知事实） | Windows DPAPI 加密存储（tauri 侧 20 行内），或至少写入时打掩码日志 | 中，隐私敏感 |
| 7 | 画布持久化整份 JSON 重写（persist.ts externalize 异步深走） | 大画布（>20MB）改分片/增量写；已有 saveSeq 守卫，缺的是速度 | 低-中 |

### 5.5 性能

| # | 问题 | 建议 |
|---|------|------|
| 1 | `collectUpstream` 在多个「无 selector 的 useBoard 订阅」里每次 store 变化都全量递归（拖拽/快捷键都触发） | 按 nodes+edges 指纹缓存上游结果（内容不变不重算） |
| 2 | ModelPicker 每次渲染 flatMap 全部服务商 | 用 useMemo 包 entries |
| 3 | 大画布下 React Flow 拖拽偶发掉帧 | 继续清查节点内未走 Thumb 的原图（ImageNode 已确认干净，抽查其余媒体节点） |
| 4 | 生成结果 dataURL 大量驻留内存（节点 results + 画廊 + 资产库缩略图三份） | 画廊/资产库可存文件路径引用，画布节点保留 dataURL 为主版本 |

### 5.6 功能补充（与前文 §5.3 合并看待）

| 建议 | 价值 | 备注 |
|------|------|------|
| **对比视图**：同一节点多次生成结果并排灯箱对比（同 prompt 多 seed/多模型） | 高 | 挑图是高频动作，现在只能点缩略图切换 |
| **提示词历史库**：跨节点检索所有用过的提示词，点击回填 | 中 | 复用资产库的搜索基建 |
| **批量费用预估**：批量生成前汇总预估费用（budget 已有单次预估能力，缺汇总） | 中 | 批量出片刚需 |
| **窗口状态记忆**：窗口位置/大小/全屏重启恢复（tauri-plugin-window-state） | 低 | 体验小加分 |
| **导出带元信息**：图片保存时把提示词/模型/seed 写入 PNG 文本块（tEXt） | 中 | 素材流转后信息不丢 |

### 5.7 明确不建议做的（避免过度设计）

- **多语言**：产品中文定位，暂无必要
- **多窗口/分屏工作区**：Tauri 单窗架构改动大，收益低
- **插件系统**：用户量级未到，协议/预设机制已覆盖扩展诉求
- **云同步**：本地优先是产品优势，数据在 AppData，备份机制已够

---

## 七、图像预处理：一张图 → 深度图/姿势图/线条/语义分割（新增 · 2026-08-13）

**需求**：传入一张图片，一步得到深度图（depth）、姿势骨架图（openpose）、模糊线条（HED soft-edge）、精确线条（canny/lineart）、语义分割（seg），作为传给生成模型的参考图。要求：打包不大、好用、精细度高、一步得到。

### 7.1 方案对比

| 方案 | 打包体积 | 精细度 | 依赖 | 结论 |
|------|---------|--------|------|------|
| **A. ComfyUI 预处理器模板包（推荐）** | **≈0（6 个小 JSON，<50KB）** | 最高（comfyui-controlnet-aux 专业工具链：MiDaS/DepthAnything/OpenPose/HED/Lineart/SAM） | 用户已装 ComfyUI（项目本就支持）；深度/姿势类需装主流插件 comfyui-controlnet-aux | ✅ 主方案 |
| B. 本地 ONNX/Rust 实现 | Canny/线稿纯算法 = 0；MiDaS-small ≈ 40MB；MoveNet 姿势 ≈ 10MB；SAM 语义分割太大（vit-b ≈ 90MB）不可行 | 中（小模型精度有限） | Rust 端 + 模型内嵌（同超清 ONNX 模式） | 兜底：仅 canny 这类纯算法值得做 |
| C. 在线 API | 0 | 取决于服务商 | 中转站几乎没有预处理接口，不可控 | ❌ |

### 7.2 推荐实现（方案 A）——「预处理」模板包 + 现有引擎复用

> **（2026-08-13 更新）此方案已确认走 ComfyUI 路线；模板与 API 由用户自行处理，MOMO 侧暂不开发预处理模板包。** 下方实现细节仅作记录：若将来需要 MOMO 内置打包，可直接复用现有 ComfyUI 节点 + 模板引擎，几乎不用写新引擎。用户在 ComfyUI 里配好预处理工作流后，通过现有「模板导入」即可在 MOMO 中调用。

项目已有全部地基，**几乎不用写新引擎**：

1. **内置 6 个预处理模板**（ComfyUI API 格式 JSON，每个 <10KB）：输入 = LoadImage 占位节点，输出 = SaveImage；
   - 精确线条：Canny（ComfyUI 原生核心节点，无需插件）
   - 模糊线条：HEDPreprocessor（controlnet-aux）
   - 精细线稿：LineartPreprocessor（controlnet-aux）
   - 深度图：MiDaS / DepthAnything（controlnet-aux）
   - 姿势骨架：OpenposePreprocessor（controlnet-aux）
   - 语义分割：SAM / CLIPSeg（controlnet-aux 或原生 + 模型）
2. **复用现有链路**：`runComfyTemplate` 已支持「上游图片自动填 LoadImage → 跑工作流 → 结果图回画布」（comfy.ts 的 upstreamImages 与 2a 步骤）——预处理模板天然适用。
3. **UI**：工具坞「预处理」按钮 = 建一个 ComfyUI 节点并自动绑定对应模板（或 ComfyConfigPanel 加「预处理模板」分组）；节点上类型选择器（6 种）+ 参数（canny 阈值 / 深度强度等按模板暴露）。
4. **一步得到**：图片节点 → 连线到预处理节点 → 点运行 → 结果图出现在节点上，可直接连到生成节点当参考（与「多输入映射」§3.4 正好配合：深度图精确接进 ControlNet 深度口）。
5. **检测兜底**：运行时探测用户 ComfyUI 是否装了 controlnet-aux（或提交失败报错里带「缺插件」提示 + 一键打开安装说明）；Canny 是原生节点，永远可用，作为「精确线条」的最低保证。
6. **纯本地兜底（远期）**：Rust + OpenCV 实现 canny/灰度/线稿（零模型、零打包），不依赖 ComfyUI 的极简路径。

**配套小需求**：导入模板包时自动把预处理模板标为「预处理」分组，不混在用户自己的模板里。

---

## 八、PNG 元信息写入（提示词/模型/seed 随图保存）

**目标**：保存图片时把提示词/模型/seed/生成时间写进 PNG 的 tEXt 文本块——图发到哪信息都不丢；未来可做「导入图片时读回提示词」。

**实施方式（对现有功能零影响）**：
1. 在 PNG 字节流的 IDAT 之前插入 tEXt chunk（key：`momo-prompt` / `momo-model` / `momo-seed` / `momo-time`）——纯 Uint8Array 操作，**不重编码图像数据**，画质零损失、体积增量可忽略；所有看图软件/平台都兼容（PNG 规范标准块）。
2. 改动点仅限保存路径（`imageSaver.ts` 的 PNG 分支）：dataURL → bytes → 插 chunk → 写盘（Tauri 改 writeFile 二进制写，浏览器走 Blob 下载）。
3. **设置页「图片保存」加开关**：「写入元信息（提示词/模型/seed）」，默认开；关掉则行为与现在完全一致。
4. 不做的：JPEG/WebP 的 EXIF 写入（JPEG APP 段与 WebP EXIF chunk 各要一套解析，先只做默认格式 PNG）。
5. 读取端（导入图片自动读回提示词填进节点）作为后续可选功能，不影响写入方案。

---

## 九、路线图（按优先级，2026-08-13 定）

**P0 · 本轮已完成**：灯箱缩放平移（滚轮/拖动/双击/工具条）、思考模式开关、消息框选右键建节点、Ollama 重复卡、历史画布弹窗越界、快捷键补齐、白屏根因修复 + ErrorBoundary 兜底。

**P1 · 已全部完成（2026-08-13）**：
1. ✅ ESLint + react-hooks 规则（`pnpm lint`，故意违规已验证可拦截）
2. ✅ RUNNERS 双表合并（runner.ts 导出 RUNNABLE_KINDS，SmartCanvas 引用）
3. ✅ 快捷键冲突检测（实时标红 + 顶部警告条；录制拦截原有保留）
4. ✅ ComfyUI 多输入精确映射（§3.4 方案 A：imageSlotMap 数据模型 + 引擎 slotMap 优先分配 + 「输入映射」弹卡 + 节点徽章 + NONE 哨兵）
5. ~~图像预处理模板包~~（已确认：模板与 API 由用户自行处理，MOMO 侧仅需保证模板导入/调用链路顺畅，无需新开发）

**P2 · 已全部完成（2026-08-13 同日）**：
1. ✅ PNG 元信息写入（iTXt 文本块 + 「图片保存」设置开关，默认开）
2. ✅ 对比视图（节点多结果缩略图行「对比」按钮 → 网格灯箱，点图切放大模式）
3. ✅ 批量费用预估（画布「全部运行」确认框附预估金额；导演台批量文案注明计费范围）
4. ✅ 3D 导演台站位参考：机位预设（全景/中景/特写/正打/反打/侧面/俯拍，以第一个角色为主体一键摆位 + 切取景视角）；人物模型库（8 体型 + 群众 + 姿势预设）与站位图导出（彩色/深度/分区）**原本已具备**，未重复开发
5. ✅ ComfyUI 词典中文化扩充（CLASS_ZH 70→170+ 词、INPUT_ZH 55→160+ 词）+ 子工作流分支自动中文命名（出图·图生/文生·放大/控制/视频等特征组合，重名自动去重）

**P3 · 已全部完成（2026-08-13 同日）**：
1. ✅ 资产库回收站（删除先进回收站 30 天可恢复；回收站页签：恢复/彻底删除/清空；超期启动时自动清理）
2. ✅ 资产库批量导出（选文件夹复制 + momo-meta.json 元信息）
3. ✅ 资产库网格内双击重命名
4. ✅ 资产库组卡右键（组内全部放画布 / 删除整组）
5. ✅ 资产库收藏置顶（卡片星标 + 「收藏」页签）
6. ✅ 导演台队列进度条（批量生成可视化进度 + 取消）
7. ✅ 导演台片段缩略图看板（列表带最新 Take 封面）
8. ✅ 参考图语义扩展（角色参考/景别参考/光线参考）
9. ✅ 撤销快照瘦身（30 步内图片完整，更早步骤保留结构/文本，大图字段置空）
10. ✅ API Key DPAPI 落盘加密（Rust CryptProtectData，绑定本机用户；内存明文无感，解密失败提示重填；浏览器预览降级明文）
11. ✅ 窗口位置/大小记忆（tauri-plugin-window-state 已注册，确认生效）
12. ✅ collectUpstream 重复调用合并（Comfy 参数栏两处订阅合一）

---

## 六、本轮已完成清单（代码已改，可直接体验）

1. ✅ 创作助手头部新增**思考模式开关**（🧠 按钮，仅助手生效；Ollama 下发 think:false、本地 GGUF 下发 chat_template_kwargs，远程协议不受影响）
2. ✅ 创作助手消息**可框选文字**，右键菜单：把选中文字建成**提示词/备注节点**、复制选中文字、整条在画布生图
3. ✅ 模型配置**重复 Ollama 卡修复**（已配置时固定卡自动让位）
4. ✅ **全屏下画布历史弹窗不再越界**（视口居中）
5. ✅ 导演台左侧导航改为**竖排大图标**（垂直于界面高度的大按钮，均分高度）+ 底部**资产库**按钮（可直接打开资产库）
6. ✅ 快捷键：新增「**打开导演台** Ctrl+Shift+D」；补齐一批空绑定的节点快捷键（视频 6、音频 Alt+1、生成音频 Alt+0、视频配音 Alt+V、拼接 Alt+C、电商长图 Alt+6、分镜 Alt+7、导演台 Alt+5、超清放大 Alt+8、智能矢量 Alt+9）；释放已废弃的 5/7 绑定；**快速添加菜单每项显示键帽**
7. ✅ 白屏根因修复 + 全仓 hooks 违规排查 + 底部参数栏 ErrorBoundary 兜底（详见 §5.1）
