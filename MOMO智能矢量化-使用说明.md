# MOMO 智能矢量化 · 使用说明

> 本地 VTracer 位图转矢量 SVG，可导出 **SVG / PDF / AI / CDR**。配套「超清增强」组成 图→高清→矢量→印刷 全链路。

## 一、它能做什么

把画布上的位图（Logo、打卡框、扁平插画、文化墙元素、线稿）用**本地 VTracer**（纯 Rust、CPU、不走云）转成矢量 SVG，再一键导出给印刷工作流：
- **SVG**：直存矢量文件。
- **PDF / AI**：经本机 **Illustrator** 自动化导出（矢量、原生）。
- **CDR**：经本机 **CorelDRAW** 自动化导出（原生）。

适合你"海报/文化墙/打卡装置 → 转矢量 → 印刷/Illustrator/CorelDRAW 二次编辑"的场景。

## 二、入口
- 底部菜单 / Spotlight：**「智能矢量化」**节点（编辑分组）。
- **右键图片** → 编辑处理 → **智能矢量化**。
- 生图节点 **编辑按钮** → **智能矢量化**（对生成图二次处理）。

## 三、怎么用
1. 把一张图片连到「智能矢量化」节点的左侧入口。
2. 选参数：
   - **类型**：**自动（默认）**/ 海报色块 / 黑白 / 线稿 / 照片。「自动」会读上游超清节点的 analysisMap 内容类型自动选预设（photo→照片档，海报/插画→海报色块档）。
   - **颜色精度**：0=自动；越大颜色越细（路径也越多）。
   - **去碎片**：0=自动；过滤掉很小的色块。
3. 点「矢量化」→ 节点显示 SVG 预览（棋盘格底看透明）+ 报告（`N 条路径 · W×H · X.Xs`）。
4. 预览下方导出按钮：**SVG / PDF / AI / CDR**（按你装了什么软件，对应按钮才可用）。

## 四、导出按钮什么时候能用
| 格式 | 条件 | 说明 |
|---|---|---|
| SVG | 始终可用 | 本地直存矢量文件 |
| PDF | 需 Illustrator | Illustrator 打开 SVG → 另存 PDF（矢量） |
| AI | 需 Illustrator | Illustrator 原生 .ai（PDF 兼容） |
| CDR | 需 CorelDRAW | CorelDRAW 新建文档 → 导入 SVG → 另存 .cdr |

- 应用是否安装是**自动探测**（查注册表 COM ProgID），没装则按钮禁用。
- 导出会**启动对应软件**（后台，最多等 180 秒）；转换中请勿操作该软件弹窗。
- 转换有**超时保护**（超时自动 taskkill 杀进程树，不卡死）。
- 导出后**校验文件头**（AI/PDF 必须以 `%PDF` 开头），防止"改扩展名"假导出。

## 五、与超清增强的链路
推荐：**图片 → MOMO 超清增强 → MOMO 智能矢量化**。
- 超清节点放大 + 输出质量分析（含**色块占比 flatRatio**：海报/文化墙高、照片低）。
- 矢量节点取上游图直接转；类型选对（色块图选"海报/色块"，线稿选"线稿"）效果最好。

> 注：矢量化是**终端节点**（产物是 SVG 文件，不把 SVG 当位图串到下游，避免喂给超分解码失败）。

## 六、诚实的边界
- ✅ 已做并自测：VTracer 转 SVG（cargo test 验证合法 SVG + 安全清洗）；SVG/PDF/AI/CDR 导出代码（编译 + 脚本结构测试）；导出能力探测。
- ⚠️ **需你验收**：PDF/AI/CDR 的实际导出（会启动你的 Illustrator/CorelDRAW，无法在自测里替你跑）——装了软件点一下按钮即可。
- ⚠️ **VTracer 输出质量**：参数（颜色精度/去碎片/类型）对每张图最优值不同，请用你的真实海报/文化墙图试，调到路径数和清晰度都合适。
- ⏳ **未做（更大工程，需视觉调参）**：按内容自动分区路由不同算法、几何图元识别（圆/矩形替代 path）、SDF 文字边缘重建、光晕抑制、vectorGuide/analysisMap 资产契约——这些是文档"阶段二/三"的内容，属于多会话 + 需你眼睛验收的进阶项。

## 七、排错
| 现象 | 处理 |
|---|---|
| AI/PI/CDR 按钮灰色 | 没探测到对应软件；确认已装且为标准版（非精简版），重启应用 |
| 导出超时 | 软件弹窗阻塞（首启欢迎屏/登录）；在软件里关掉"启动时显示欢迎屏"后重试 |
| AI 导出报"头不是 %PDF" | Illustrator 保存选项异常；重试或在 Illustrator 里手动另存核对 |
| 路径过多/过碎 | 调高「去碎片」、调低「颜色精度」、或类型改"少色块" |
| 照片转矢量效果差 | 正常——照片不适合纯矢量；建议"保留照片+外框转矢量"（暂未做，可对照片区裁剪后再转） |

## 八、技术一句话
Rust：`vec.rs`（VTracer convert_image_to_svg + 清洗）+ `export.rs`（cscript 跑内嵌 VBScript 驱动 Illustrator/CorelDRAW COM + taskkill 超时 + 文件头校验 + 注册表探测）。前端：`VectorizeNode.tsx` + `runVectorize`（runner）。
